# 📋 Índice de hallazgos de auditoría

Índice único de los IDs de hallazgo (`SEC-1`, `SEC-10`, `OFF-3`, `ARCH-11`, etc.) que
aparecen dispersos en comentarios de código y migraciones. Antes de este
documento, ubicar qué era un ID específico requería `grep` sobre todo el repo.

**Metodología:** cada fila se verifica contra el código/BD real (`grep`,
`git log`, `pg_policies`, `vite build`), nunca contra un informe externo sin
confirmar. Al cerrar un hallazgo nuevo: usar el formato ya establecido en el
repo (`// Fix <ID> (auditoría <fecha>): qué y por qué` en código, `-- Migración
NNNN — Fix <ID>: resumen` en SQL), agregar/actualizar su fila aquí, y si
reabre o profundiza un hallazgo anterior decirlo explícitamente.

**Nota de proceso (agregada tras `UX-19`, 14 de julio):** las auditorías de
arquitectura ya chequean código duplicado entre componentes, pero no
*patrones de UI/UX repetidos entre los 3 módulos raíz*
(`HorariosLayout`/`AsistenciasModulo`/`AdminModulo`) — la misma acción de
usuario resuelta distinto en cada módulo no aparece en ningún grep de código
duplicado porque el comportamiento diverge, no el código. Desde la próxima
auditoría: por cada elemento de navegación común a los 3 módulos raíz
(topbar, dropdown de usuario, back-buttons, badges, atajos), confirmar que
los 3 usan el mismo componente/clase o que la diferencia está documentada
como intencional.

**Esquema de IDs (normalizado el 13-14 de julio de 2026):** 8 prefijos, uno
por área, sin colisión — `SEC-N` (seguridad y RLS), `PERM-N` (filtrado por
permiso/programa), `OFF-N` (offline y red), `ARCH-N` (arquitectura, testing y
concurrencia), `UX-N` (UI y estilos), `DESIGN-N` (identidad visual), `CI-N`
(CI/CD) y `ADMIN-N` (funcionalidad nueva pedida por el usuario, no hallazgo
de auditoría — se incluye acá porque el código usa el mismo formato de
comentario). Antes de esta fecha convivían 8-10 esquemas superpuestos (`A1`
sin guion, `A-2` con guion y `ARCH-4` eran tres cosas *distintas*) — ver
**Tabla de equivalencias** al final si un ID citado en un commit/PR viejo no
aparece arriba. IDs mencionados en código pero nunca localizados (esquema
antiguo, probablemente descartados antes de llegar a `main`): `O-6`, `O-7`,
`P-1`, `S1`, `SEC-4` (viejo). El proyecto también usó `Fix #N`/`Gap #N`,
retiradas antes de este índice — ver § Esquema retirado al final. Para el
esquema de BD y migraciones SQL, ver `ESQUEMA_Y_MIGRACIONES.md`.

---

## 🔴 Hallazgos realmente abiertos

**`SEC-37` ⛔ no aplicable, no es hallazgo pendiente.** El toggle de
leaked-password protection requiere plan Pro (organización real
`bolysrglhpxjfydtuzkw`, plan `free`); LS confirmó (16 ago) que no
tiene planes de contratar Pro en el corto plazo — decisión de
producto, mismo criterio que `UX-13` (modo oscuro revertido), no un
bloqueo a resolver. Reevaluar solo si el plan cambia en el futuro.
Detalle en la fila `SEC-37`.

**2 abiertos, ambos hardening no urgente sin explotación conocida —
`SEC-39 (1)/(2)`:** 3 funciones sin `search_path` fijo (`get_my_role`,
`get_my_programa`, `limpiar_scan_rate_limit` — reducido de ~15 desde
el 11 ago, sin quedar registrado quién corrigió el resto) y la
extensión `pg_trgm` en el schema `public` en vez de uno dedicado.
Ambos reverificados 16 ago contra el proyecto real. El tercer punto
de `SEC-39` (firma duplicada de `restaurar_backup()`) y `SEC-40`/
`SEC-41` (RLS de `horarios` bloqueando trimestres cerrados a nivel
de BD) se verificaron cerrados el 16 ago — ver esas filas.

**Patrón recurrente a vigilar:** dos veces un hallazgo se marcó ✅
cerrado sin que el fix llegara realmente al código/CI (`ARCH-29`→`31`,
2 ago; `ARCH-34`→`35`, 3 ago; mismo patrón de fondo en `ARCH-43`,
10 ago) — siempre detectado verificando archivo por archivo contra el
HEAD real, nunca dando por buena la tabla de este documento sin más.
El sistema de Sedes tuvo el mismo problema (`ARCH-37`, § Sistema
multi-sede).

`UX-13` (modo oscuro) sigue ⛔ revertido a pedido de LS — decisión de
producto, no hallazgo pendiente. CodeQL corre en cada push/PR y
semanalmente por cron (`SEC-20`) — revisar Security → Code scanning
periódicamente, 0 hallazgos hoy no es un estado permanente.

Rate limiting nativo de Auth (`SEC-7`), `pg_cron` (`SEC-21`) y grants
de las 8 funciones admin/sesión (`SEC-9`/`SEC-17`) confirmados sin
drift el 9 ago — detalle completo, la auditoría de estrés operacional
del 10 ago (`ARCH-43`/`OFF-12`/`SEC-38`/`UX-35`) y el drift paralelo
de esos días (4 migraciones renumeradas `0082`–`84`→`0085`–`88`): ver
§ Historial cronológico y las filas de cada ID en sus tablas.

**Cerrado hoy, `UX-47`/`UX-48` (15 ago, verificación E2E de UI/UX
independiente):** trampa de foco de teclado ausente en `UploadPreviewModal`/
`ModalCambiarPassword` y cierre por `Escape` ausente en los dropdowns
`AdminMenu`/`UserMenu` — ver filas `UX-47`/`UX-48`. De paso se detectaron
y corrigieron **3 colisiones de ID reales** (mismo patrón que `SEC-33`/
`UX-33` ya resolvió antes): un fix de contraste (`--role-text`) comentado
como `UX-42` pisaba el `UX-42` ya documentado (targets táctiles) →
renumerado `UX-49`; la vista previa sticky de `ConfiguracionReportes` y
el fallback de contraste `.role-color--default` estaban ambos comentados
como `UX-41`, pisando el `UX-41` real (scroll de backdrops de modal) →
renumerados `UX-50`/`UX-51`. También se agregaron a este índice
`UX-44`/`45`/`46`, implementados en código desde el 14-15 ago pero nunca
indexados (`UX-41` sí estaba indexado, solo fuera de orden numérico).
Detalle completo del incidente de subida y su corrección: § Historial
cronológico, entrada del 15 ago.

**Pendientes previos reconciliados (15 ago):** `UX-35` (baselines
WebKit `mobile-webkit-375`/`tablet-webkit-768`, generadas el 11 ago en
`8371a62`) y la graduación a bloqueante del CI de WebKit/axe-core
(`847daa4`, 12 ago, corrida real `85855045178` — 9/9 + 3/3 sin falsos
positivos) ya estaban cerrados antes de esta sesión; las notas de
"pendiente" que este índice todavía arrastraba quedaron corregidas en
`UX-35`/`UX-39`. **Sin hallazgos de UI/UX abiertos tras esta
reconciliación.** `SEC-37` reclasificado (16 ago) como no aplicable
por decisión de LS, no bloqueo pendiente. `OFF-10` verificado cerrado
(16 ago, migración `0075` confirmada en producción). `SEC-40`/`41`
(RLS de `horarios` en trimestres cerrados) encontrados aplicados en
producción sin registrar en este índice — documentados retroactivamente
el 16 ago, ver esas filas. `ARCH-39` cerrado el mismo día (16 ago):
primera corrida real con los 3 secrets presentes, 0 errores inesperados
en ambas oleadas — ver esa fila para el detalle de `DEVICE_DUPLICADO`
(esperado, no es una falla).

---

## 🔐 Seguridad y RLS

Esquema `SEC-N`. Fusiona lo que antes eran 4 esquemas paralelos (`S-N`,
`SEC-N`, `V-N`, `D-N`) — ver tabla de equivalencias al final.

| ID | Descripción | Archivo(s) clave | Migración | Estado |
|---|---|---|---|---|
| **SEC-1** | RLS nunca habilitado en tabla padre particionada `horarios` — cualquier autenticado podía escribir cualquier programa | `horarios` (padre + particiones) | `0035`, `0045` | ✅ Cerrado |
| **SEC-2** | `docentes`/`materias`: política `FOR ALL` solo exigía rol autenticado, sin permiso granular (severidad reportada externamente mayor a la real — RLS y bloqueo `anon` ya activos) | `docentes`, `materias` | `0046` | ✅ Cerrado |
| **SEC-3** | Estilos inline bloqueaban CSP estricta | Todo `src/` — ver `UX-5` | — | ✅ Cerrado (5 jul) — `unsafe-inline` retirado de `style-src`; `ModalRol.jsx` restringido a 10 presets de color |
| **SEC-4** | Stack trace completo visible en producción | `ErrorBoundary.jsx` | — | ✅ Cerrado (solo se renderiza en dev) |
| **SEC-5** | Sin validación centralizada de fortaleza de contraseñas | `src/utils/password.js` | — | ✅ Cerrado |
| **SEC-6** | Lockout de login en `localStorage` no resistía pestañas privadas | `LoginScreen.jsx`, `pinOffline.js` | — | ✅ Cerrado (migrado a IndexedDB) |
| **SEC-7** | Sin respaldo server-side del lockout de `SEC-6` | `LoginScreen.jsx`, RPC `verificar_bloqueo_login` | `0047` | ✅ Cerrado. No reemplaza el rate limiting nativo de Supabase Auth — verificar que siga activo en el dashboard |
| **SEC-8** | `login_attempts` con INSERT abierto a `public` (`WITH CHECK (true)`) — cualquiera podía forzar bloqueo de otra cuenta | `login_attempts` | `0048` | ✅ Cerrado |
| **SEC-9** | 4 funciones con `REVOKE ALL` original pero ejecutables por `anon` en BD real (drift) | 4 RPCs (ver historial) | `0049` | ✅ Cerrado |
| **SEC-10** | INSERT/DELETE de `horarios` sin permiso granular | `horarios` | `0035` | ✅ Cerrado (misma causa raíz que `SEC-1`) |
| **SEC-11** | RLS de `qr_sessions`/`asistencias_diarias` sin permisos granulares | `qr_sessions`, `asistencias_diarias` | `0036` | ✅ Cerrado |
| **SEC-12** | `crear_qr_session()` solo validaba rol, no permiso `puedeGestionarQR` | RPC `crear_qr_session` | `0035` | ✅ Cerrado |
| **SEC-13** | Sin rate limiting en `registrar_asistencia()` | `registrar_asistencia`, `scan_rate_limit` | `0039`, `0040` | ✅ Cerrado |
| **SEC-14** | 2 CVEs "alta severidad" reportadas para `xlsx` | `package.json` | `0.20.3` | ✅ Cerrado — falso positivo (verificado contra advisories oficiales de SheetJS): ambas ya corregidas antes de `0.20.3`; `package.json` apunta al tarball oficial, no al paquete npm abandonado. No se migra a `exceljs` |
| **SEC-15** | `admin_caller_puede_gestionar_usuarios()` no comparaba rol actor vs. objetivo — escalada de privilegios (cualquier rol con el permiso podía crear/editar admins) | 5 RPCs `admin_*`, `api/admin-users.js` | `0050` | ✅ Cerrado — helper `admin_caller_es_admin()` como guard en las 5 RPCs y en `admin-users.js` |
| **SEC-16** | `api/admin-users.js` (Service Role Key) sin rate limit propio | `api/admin-users.js`, `admin_actions_rate_limit` | `0051` | ✅ Cerrado — 10 acciones/min por `actor_id` |
| **SEC-17** | 4 RPCs de sesión ejecutables por `anon` sin `REVOKE` explícito (mismo patrón `SEC-9`) | 4 RPCs de sesión | `0052` | ✅ Cerrado — resuelto vía `pg_proc` real, no asumido |
| **SEC-18** | `npm audit`: 2 CVEs en `vite`/`esbuild`, solo afectaban el dev server | `package.json` | — | ✅ Cerrado (11 jul) — `vite@^6.4.3` (dentro del rango soportado por los plugins instalados, evita el salto mayor que rompía el build) |
| **SEC-19** | `api/admin-users.js` sin cabeceras CORS propias | `api/admin-users.js` | — | ✅ Cerrado (12 jul) — validación de `Origin` vs `req.headers.host`, 403 si no coincide |
| **SEC-20** | Sin SAST sobre código propio en CI | `.github/workflows/codeql.yml` | — | ✅ Cerrado (13 jul) — job CodeQL separado y no bloqueante. **Ver `SEC-23`**: primera corrida real aún sin confirmar |
| **SEC-21** | Sesión nunca expiraba sola (persistSession + timeout solo en memoria del componente) | `useAuth.js`, `auth.sessions` | `0053`, `0055` | ✅ Cerrado (10 jul) — 2 capas: client (timeout persistido en `localStorage` + time-box 10h) y server (`pg_cron` cada 15min purga sesiones vencidas). Pendiente en dashboard Supabase (no es migración): confirmar `pg_cron` habilitado |
| **SEC-22** | Sin política documentada de rotación de `SUPABASE_SERVICE_ROLE_KEY` | `docs/SECURITY.md` | — | ✅ Cerrado (13 jul) — sección nueva con casos de rotación y pasos concretos |
| **SEC-23** | Ver detalle en `SEC-25` — la corrida real de CodeQL que faltaba confirmar ya ocurrió | `.github/workflows/codeql.yml` | — | ✅ Cerrado (16 jul) — primera corrida real de CodeQL flageó 2 alertas High ("DOM text reinterpreted as HTML" vía `document.write()`), ambas triadas y resueltas — ver `SEC-25` |
| **SEC-24** | La CSP de `vercel.json` es estricta pero no tenía endpoint de reporte (`report-uri`/`report-to`) | `vercel.json`, `api/csp-report.js` | — | ✅ Cerrado (15 jul) — endpoint público sin auth (inserta en `audit_logs` vía Service Role), rate limit 20 req/min por IP (best-effort en memoria, no persistente entre instancias serverless), 8 tests nuevos |
| **SEC-25** | Las 2 alertas High de la primera corrida real de CodeQL (`SEC-23`), ambas "DOM text reinterpreted as HTML" vía `document.write()`, investigadas por separado en vez de asumir un veredicto compartido | `exportPDF.js`, `PlanillaImprimibleBase.jsx` | — | ✅ Cerrado (16 jul) — **`exportPDF.js`: falso positivo parcial** (mismo patrón que `SEC-2`/`SEC-14`): ya escapaba casi todo con su `ESC()` local; el único hueco (`programa` en `exportarPDFDiario`) solo puede venir de un `<select>` de opciones fijas, nunca texto libre — no explotable en la práctica, pero se escapó igual por defensa en profundidad. **`PlanillaImprimibleBase.jsx`: vulnerabilidad real** — sin ningún escapado; docente/materia/sección/programa (datos reales de cargas masivas de Excel vía `useUpload.js`) se interpolaban crudos en el HTML de `document.write()` — XSS almacenado de segundo orden explotable con un nombre de docente/materia cargado con HTML/script. Mismo helper `ESC()` agregado, las 4 interpolaciones escapadas. 2 tests de regresión nuevos confirmando que un payload `<script>`/`<img onerror>` llega escapado al HTML impreso. 181/181 tests, 0 errores de lint, build limpio |
| **SEC-26** | `npm audit --omit=dev --audit-level=high` en CI fallaba con 3 vulnerabilidades (2 High: `brace-expansion` DoS, `svgo` `removeScripts` incompleto; 1 Moderate: `tar` recursión no acotada) | `package.json`, `package-lock.json` | — | ✅ Cerrado (1 ago) — ninguna de las 3 viene de una dependencia real del proyecto: las tres son transitivas de `svgtofont` (herramienta de build interna de `@tabler/icons-webfont`, declarada como `dependencies` de ese paquete, no `devDependencies`). Verificado que sigmapnf **no usa el paquete npm en absoluto** — los íconos se sirven desde `public/fonts/tabler-icons.min.css`/`.woff2`, archivos estáticos ya generados y versionados en git, enlazados directo desde `index.html` (`grep` exhaustivo fuera de `node_modules`: cero referencias en JS/CSS/scripts de build). El paquete quedó en `dependencies` desde que se usó una vez para generar esos archivos. Fix: `npm uninstall @tabler/icons-webfont`. `npm audit --omit=dev --audit-level=high` → 0 vulnerabilidades, exit 0. 185/185 tests, 0 errores de lint, build idéntico (mismo `dist/`, mismo precache de PWA) |
| **SEC-27** 🟢 | Carga de Excel (`useUpload.js`) valida el archivo solo por extensión/mimetype del navegador, sin verificar el contenido real antes de pasarlo a `XLSX.read()` — un archivo renombrado a `.xlsx` sin serlo llega hasta el parser | `useAppData/useUpload.js` | ✅ Cerrado (2 ago) — chequeo de magic bytes antes de invocar el parser: `.xlsx` valida firma ZIP (`PK\x03\x04`/`\x05\x06`/`\x07\x08`), `.xls` valida firma OLE2/CFB (`D0 CF 11 E0 A1 B1 1A E1`); lectura de los primeros 8 bytes vía `FileReader.readAsArrayBuffer()` (no `Blob.slice().arrayBuffer()` — incompatible con jsdom/algunos entornos), mensaje accionable si no coincide. 2 tests de regresión nuevos (rechaza contenido falso sin invocar al parser, acepta `.xlsx` real). 213/213 tests, 0 errores de lint, build limpio |
| **SEC-28** 🔴 | `admin_borrar_asistencias_rango()` (`SECURITY DEFINER`, se salta el RLS por sede de `0063`) validaba `puedeBorrarReportes` pero no la sede del actor — un rol con ese permiso en UNA sede podía borrar `asistencias_diarias` de TODAS las sedes por rango de fecha/turno/programa | `admin_borrar_asistencias_rango` (`0054`) | `0068` | ✅ Cerrado (6 ago) — ver `SEDE-13`: encontrado en paralelo por una auditoría externa y por LS el mismo día; la migración real que lo cierra es la de `SEDE-11`–`14` |
| **SEC-29** 🔴 | `admin_borrar_qr_sesiones()` (`SECURITY DEFINER`) borraba por array de UUIDs sin verificar que pertenecieran a la sede del actor — mismo patrón que `SEC-28`, menos explotable (requiere conocer IDs de otra sede) pero mismo hueco de diseño | `admin_borrar_qr_sesiones` (`0054`) | `0068` | ✅ Cerrado (6 ago) — ver `SEDE-13`. `admin_borrar_session_logs()` se revisó y queda fuera de alcance a propósito: `session_logs` (`0031`) no tiene columna `sede_id` |
| **SEC-30** 🔴 | Reportado por LS al toparse con `Could not find the function admin_upsert_user_profile(...)`: la migración `0062` (la que le agrega `p_sede_id` a ese RPC) nunca se había corrido en producción — LS la corrió a mano vía SQL Editor. Al verificar el resto de RLS con LS en el camino, se encontraron 2 políticas SELECT **huérfanas**, creadas a mano fuera de cualquier migración, activas en paralelo con las correctas de `0064`: `permiso_lee_qr_sessions` (`qr_sessions`) y `permiso_lee_asistencias` (`asistencias_diarias`), ninguna con `usuario_puede_ver_sede()`. Por el OR de políticas permisivas de Postgres, anulaban el filtro de sede de la política correcta — cualquiera con `puedeGestionarQR`/`puedeVerReporteAsistencias` veía sesiones/asistencias de **todas** las sedes en la base real, aunque el código de la app ya filtrara bien | `qr_sessions`, `asistencias_diarias` (políticas RLS, creadas fuera de migraciones) | `0071` (formaliza el `DROP` ya aplicado a mano el 7 ago) | ✅ Cerrado (7 ago) — LS confirmó con `pg_policies` que ambas políticas huérfanas quedaron eliminadas; `0071` versiona el mismo `DROP POLICY IF EXISTS` (idempotente) para que quede en el historial reproducible, ya que el origen del problema fue exactamente que estas políticas nunca pasaron por una migración |
| **SEC-31** 🔴 | Encontrado durante la auditoría completa de `pg_policies` que destapó `SEC-30`: `session_logs` tiene desde la migración `0031` (mucho antes del sistema de sedes) DOS políticas `INSERT` activas — `sl_no_insert_directo` (`WITH CHECK false`, pensada para forzar que todo INSERT pase por el RPC `log_session_event()`) y una zombi `sl_insert` que **nunca fue borrada por ninguna migración**, usa el sistema de roles viejo `get_auth_role()` (reemplazado por `tiene_permiso()` desde `0016`/`0025`) y permite `auth.uid() = user_id` o rol admin/coordinador. Por el mismo OR de políticas permisivas, `sl_insert` anulaba la intención de `sl_no_insert_directo`: cualquier usuario autenticado podía insertar directo en `session_logs` (la tabla de auditoría de sesiones) una fila arbitraria con su propio `user_id`, sin pasar por el RPC ni ninguna validación de contenido | `session_logs` (política `sl_insert`) | `0071` | ✅ Cerrado (7 ago) — `DROP POLICY "sl_insert"`; `sl_no_insert_directo` queda como única política INSERT, `log_session_event()` (SECURITY DEFINER) sigue siendo la única vía de escritura |
| **SEC-32** 🟡 | Encontrado en la misma auditoría: `solo_hoy_insert_qr_sessions` (INSERT de `qr_sessions`, migración `0025`, anterior al sistema de sedes) nunca tuvo `usuario_puede_ver_sede(sede_id)` en su `WITH CHECK` — quedó fuera de `0063`/`0064`, que solo tocaron el `SELECT`. Riesgo bajo en la práctica: el frontend real siempre crea sesiones vía el RPC `crear_qr_session` (`SECURITY DEFINER`, resuelve/valida la sede), nunca con INSERT directo — pero RLS es la única barrera real contra un INSERT directo por API fuera de la UI de la app, y hoy no la tenía | `qr_sessions` (política `solo_hoy_insert_qr_sessions`) | `0071` | ✅ Cerrado (7 ago) — agrega `usuario_puede_ver_sede(sede_id)` al `WITH CHECK`, mismo patrón que el resto de `SEDE-N` |
| **SEC-33** 🔴 | Auditoría completa de `EXECUTE` en TODAS las funciones `SECURITY DEFINER` (pedida a LS tras `SEC-30`/`31`/`32`, mismo día, por sospecha de que hubiera más cosas del mismo estilo): ~24 funciones administrativas sensibles — `admin_create_auth_user`, `admin_delete_user`, `admin_delete_orphan_auth_user`, `admin_reset_user_password`, `admin_delete_role`, `admin_upsert_role`, `admin_borrar_asistencias_rango`, `admin_borrar_qr_sesiones`, `admin_borrar_session_logs`, `restaurar_backup`, `borrar_horarios`, `replace_nombre_en_clases`, `get_audit_logs`, `get_session_logs`, `crear_qr_session`, `docentes_con_cedula`, entre otras — aparecen ejecutables por `anon`/`PUBLIC` sin que ninguna migración se lo haya otorgado. Mismo patrón exacto que `SEC-8`(`0049`)/`SEC-9`(`0052`): funciones tocadas directo en el SQL Editor de Supabase en algún momento, endurecimiento de grants nunca aplicado o perdido — pero mucho más grande, prácticamente toda la superficie de RPCs administrativos. Riesgo real probablemente bajo (se revisó `admin_delete_user` como muestra: lo primero que hace es el chequeo de permiso sobre `auth.uid()`, que es NULL para un caller anónimo — falla de inmediato), pero se cierra por el mismo principio ya establecido en `0052`: no depender de que `anon` "no tenga motivo". Se excluyen a propósito las 4 funciones legítimamente anónimas (`buscar_docente_scan`/`registrar_asistencia` — auto-escaneo QR sin sesión, confirmado contra el código real; `verificar_bloqueo_login`/`log_login_fallido` — se llaman antes de que exista sesión, durante el login) y las auxiliares de RLS (`tiene_permiso`/`usuario_puede_ver_sede`, necesitan ser ampliamente invocables porque las políticas las usan constantemente) | ~24 funciones `SECURITY DEFINER` (ver migración para la lista completa) | `0073` | ✅ Cerrado (7 ago) — LS corrió `0073` y confirmó con la consulta completa de grants que las 32 funciones objetivo quedaron solo `authenticated`/`postgres`/`service_role`, sin tocar las 4 legítimamente anónimas ni las auxiliares de RLS. Numerada originalmente `0072`; renombrada a `0073` al detectar que `UX-33` (trabajo paralelo) ya había tomado ese número — sin conflicto de contenido |
| **SEC-34** 🟡 | Causa raíz de `SEC-8`/`SEC-9`/`SEC-33`, encontrada al verificar el cierre de `SEC-33`: el rol `postgres` (el que usa el SQL Editor de Supabase para crear funciones) tenía una regla de privilegios por defecto (`pg_default_acl`) que otorgaba `EXECUTE` a `anon` automáticamente en toda función nueva del esquema `public` — confirmado en vivo: `contar_docentes_esperados` (`0072`, `UX-33`) salió anon-ejecutable pese a que su propia migración hace `REVOKE ALL ... FROM PUBLIC`, porque nunca tuvo el privilegio vía `PUBLIC` — lo tenía directo por esta regla, que `REVOKE ... FROM PUBLIC` no toca. Explica por qué el mismo patrón se ha repetido 3 veces en la historia del proyecto y por qué se seguiría repitiendo con cada función `SECURITY DEFINER` nueva si no se corregía la regla en sí, no solo cada función una por una | Privilegios por defecto de `postgres` en el esquema `public`; parche puntual en `contar_docentes_esperados` | `0074` | ✅ Cerrado (7 ago) — `ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public REVOKE EXECUTE ON FUNCTIONS FROM anon` corrido y confirmado con `pg_default_acl`: `postgres` ya no tiene `anon` en la ACL por defecto de funciones. El mismo comando para `supabase_admin` falló con `permission denied to change default privileges` (rol interno de Supabase, LS no tiene permiso para alterarlo) — sin impacto real, ninguna migración del proyecto crea funciones bajo ese rol, así que queda fuera de alcance sin necesidad de resolverlo. Parche puntual en `contar_docentes_esperados` también corrido (no confirmado explícitamente con una segunda query, pero corrido junto con el resto sin error reportado) |
| **SEC-35** 🟠 | Encontrado en la auditoría completa de BD (9 ago): `uq_asistencia_docente_dia_tipo` (`0008`, previo al sistema de sedes) es `UNIQUE (cedula_docente, fecha, tipo)` sin `sede_id`. `0061` estableció catálogos de docentes independientes por sede (`docentes_sede_cedula_unique` es `(sede_id, cedula)`, no global) pero este constraint nunca se actualizó — un mismo `cedula_docente` en dos sedes distintas el mismo día/tipo rechazaría el segundo registro aunque sean asistencias legítimas en sedes diferentes | `asistencias_diarias` | `0082` | ✅ Cerrado (9 ago) — constraint reemplazado por `uq_asistencia_docente_dia_tipo_sede`, `UNIQUE (sede_id, cedula_docente, fecha, tipo)` |
| **SEC-36** 🟡 | Encontrado en la misma auditoría: política `Lectura publica trimestres` con `roles={public}` (incluye `anon`), único catálogo no restringido a `authenticated` (`sedes`/`roles` sí lo están correctamente). Sin migración que la haya creado — mismo patrón de objeto fantasma que `SEC-9`/`SEC-17`/`SEC-30`/`31`. Verificado contra el código real: `trimestres` solo se consulta desde `App.jsx`/`HistorialView.jsx` (contextos autenticados); el flujo anónimo `/scan` no la toca — sin necesidad funcional de que sea pública | `trimestres` | `0082` | ✅ Cerrado (9 ago) — política reemplazada por `trimestres_select_authenticated`, `roles={authenticated}` |
| **SEC-37** ⛔ | Verificación operativa de rate limiting nativo de Auth (`SEC-7`) en el dashboard real, pedido de LS (9 ago): confirmado activo y reforzado (sign-ups/sign-ins en 4 req/5min por IP, resto en default). De paso, el advisor de seguridad de Supabase (`get_advisors`) señaló `auth_leaked_password_protection` deshabilitado — Auth no chequea contraseñas nuevas contra HaveIBeenPwned antes de aceptarlas. No es una migración ni afecta código: toggle de dashboard (Authentication → Policies), pero confirmado contra la organización real que **requiere plan Pro** — el proyecto está en plan `free` | — (config del proyecto, no código) | — | ⛔ No aplicable — LS confirmó (16 ago) que no tiene planes de contratar Pro en el corto plazo. Decisión de producto, no hallazgo pendiente; mismo criterio que `UX-13`. Reevaluar solo si cambia el plan del proyecto |
| **SEC-38** 🟡 | Auditoría de estrés operacional (10 ago, simulación externa de entradas maliciosas/erróneas): `mensajeAmigable()` (`UX-31`/`32`) era una whitelist *pass-through* — cualquier mensaje de Postgres no cubierto por sus reglas se mostraba crudo al usuario final. Ante un parámetro manipulado a mano (URL/DevTools) con tipo inválido, o una política RLS rechazando la operación, el mensaje crudo (`invalid input syntax for type uuid...`, `permission denied for table...`) filtraba nombres de tabla/columna/tipo internos — fuga de información de esquema de bajo riesgo (OWASP A05), útil para reconocimiento previo a un intento de explotación más serio, no explotable por sí sola. Verificación adicional encontró 6 archivos más que concatenaban `error.message` directo sin pasar por `mensajeAmigable()` en absoluto (`TabSesiones.jsx`, `HistorialView.jsx` ×3, `HistorialSesiones.jsx`, `ReporteRango.jsx`, `horarioEditing.js` ×2, `useDataSync.js`) | `src/utils/errorMessages.js`, + 6 archivos de call sites (ver descripción) | — | ✅ Cerrado (10 ago) — el fallback de `mensajeAmigable()` deja de devolver el mensaje original: ahora es un mensaje genérico accionable, con el original preservado solo en `logger.warn` (silenciado en producción, mismo criterio que el resto de `logger.js`). 2 reglas nuevas (`invalid input syntax/value for`, `permission denied for`) además de las 3 ya existentes. Los 6 call sites que bypasseaban el filtro ahora pasan por `mensajeAmigable()`; el audit log (`logAudit`) sigue guardando el mensaje crudo a propósito — es un registro interno para el equipo técnico, no algo que vea el usuario final. 2 tests preexistentes actualizados (verificaban el comportamiento pass-through viejo, ahora verifican el mensaje genérico), 4 tests nuevos. 411/411 tests, 0 errores de lint, build limpio |
| **SEC-39** 🟡 | Primera verificación en vivo del proyecto real vía `get_advisors`/security del conector de Supabase (11 ago). 3 hallazgos, ninguno bloqueante: **(1)** ~15 funciones sin `search_path` fijo — hardening estándar, sin explotación práctica; **(2)** extensión `pg_trgm` instalada en `public` en vez de un schema dedicado; **(3)** función duplicada — `restaurar_backup()` con 2 firmas distintas en el advisor | ~15 funciones `SECURITY DEFINER`/`INVOKER`, `pg_trgm`, `restaurar_backup` | `0098` | 🟢 **(3) cerrado** (16 ago, verificado contra `pg_proc` real): sobrevive una sola firma, coincide con la que llama `backupActions.js`. **(1) reducido de ~15 a 3 funciones** (verificado 16 ago contra el proyecto real: `get_my_role`, `get_my_programa`, `limpiar_scan_rate_limit` sin `search_path` fijo — alguien corrigió el resto entre el 11 y el 17 ago sin que quedara registrado acá) — sigue abierto, no urgente. **(2) sin cambios**, `pg_trgm` sigue en `public` — verificado 16 ago |
| **SEC-40** 🟢 | Encontrado y corregido fuera de esta sesión, sin registrar en este índice hasta ahora (reconciliación 16 ago, mismo patrón de drift documental que `UX-35`/`UX-39` el 15 ago) — confirmado por SQL real de la migración, leído directo de `supabase_migrations.schema_migrations`, no reconstruido de memoria. RLS de `horarios` (`SEDE-3`/`PROG-3`: permiso + sede + programa) nunca impedía a nivel de BD escribir sobre un trimestre ya cerrado — el banner de "modo consulta" del frontend era la única barrera, puramente de sugerencia visual, evadible por cualquier llamada directa a la API de PostgREST | `_aplicar_rls_horarios()` (función que genera las políticas RLS de `horarios` y sus 7 particiones) | `0098`, `0099` | ✅ Cerrado — `INSERT`/`UPDATE`/`DELETE` ahora exigen además `NOT EXISTS (... trimestres t WHERE t.lapso = lapso AND t.estado = 'cerrado')`. **Bug propio detectado y corregido en la migración siguiente el mismo día:** la primera versión (`0098`) escribía `t.lapso = lapso` sin calificar — con la subconsulta correlacionada a `trimestres t`, Postgres podía resolver `lapso` contra `t` en vez de la fila de `horarios`, dejando la condición siempre verdadera para cualquier trimestre existente en vez de comparar contra el de la fila real; corregido calificando explícitamente `%I.lapso` con el nombre real de la tabla objetivo (no un alias) |
| **SEC-41** 🟢 | Consecuencia directa de `SEC-40`, detectada y corregida el mismo día (también fuera de esta sesión, mismo criterio de reconciliación): el guard nuevo bloqueaba sin excepción el `UPDATE` global que hacen `unificar_docente()`/`unificar_materia()` al fusionar un docente o materia duplicado — esas funciones no son `SECURITY DEFINER` (dependen de la RLS del caller) y actualizan `horarios.docente_id`/`materia_id` sin filtrar por lapso, a propósito, para que la fusión alcance también el historial cerrado. Con `SEC-40` sin excepción, la fila quedaba sin repuntar en los lapsos cerrados y el `DELETE` posterior sobre `docentes`/`materias` fallaba por FK (`NO ACTION`) contra esas filas huérfanas — una fusión de docentes/materias rompía en cualquier institución con al menos un trimestre cerrado en el historial | `unificar_docente()`, `unificar_materia()`, `_aplicar_rls_horarios()` | `0099` | ✅ Cerrado — GUC de sesión transaccional `app.bypass_lapso_cerrado` (`set_config(..., true)`, `is_local=true`: se resetea sola al terminar la transacción, no persiste entre llamadas ni afecta otras sesiones), activada solo dentro de `unificar_docente`/`unificar_materia` justo antes de su `UPDATE` global. Distinción explícita en el propio comentario SQL: es fusión de identidad (mismo docente/materia, dos filas duplicadas), no edición de contenido — una edición real de clase/aula/horario en un trimestre cerrado sigue bloqueada sin excepción |

## 🔎 Filtrado de datos por permiso/programa

Esquema `PERM-N` (antes disperso entre `V-3` y parte de `D-N`).

| ID | Descripción | Archivo(s) clave | Estado |
|---|---|---|---|
| **PERM-1** | Pestañas de `AsistenciasModulo` no filtradas por permisos individuales | `src/app/AsistenciasModulo.jsx` | ✅ Cerrado |
| **PERM-2** | Mismo problema que `PERM-1`, en `LogsView` | `src/components/LogsView.jsx` | ✅ Cerrado |
| **PERM-3** | `HistorialView` no respetaba `restringe_programa` | `src/components/HistorialView.jsx` | ✅ Cerrado |
| **PERM-4** | `exportarDatos()` consultaba una tabla `asistencias` inexistente | `src/hooks/useAppData/backupActions.js` | ✅ Cerrado (corregido a `asistencias_diarias`) |
| **PERM-6** 🟡 | Propuesta D de una revisión externa (LS le pidió a Grok mejoras, ver `UX-33`): `puedeHacerBackup` y `puedeVerTodo` solo se controlaban en la UI, sin respaldo server-side. Confirmado contra el código real: `exportarDatos()` hacía 4 consultas directas del cliente (`horarios`/`docentes`/`materias`/`asistencias_diarias`) sin pasar por ningún RPC — el único freno real era RLS por sede (`SEDE-14`), que nunca comprobó `puedeHacerBackup` en sí. Cualquier usuario autenticado con lectura normal a esas tablas podía exportar el mismo dataset desde la consola del navegador sin tener el permiso. Alcance acotado a `puedeHacerBackup` en esta pasada — `puedeVerTodo` (ver todos los programas vs. solo el propio) queda pendiente como una serie propia (`PROG-N`, análoga a `SEDE-N`): toca muchos más puntos de lectura en toda la app y no tiene RLS por programa hoy en ningún lado, así que no es un fix de una sesión | `src/hooks/useAppData/backupActions.js` | ✅ Cerrado (8 ago, solo `puedeHacerBackup`) — nuevo RPC `exportar_backup_completo` (`0076`, `SECURITY DEFINER`) reemplaza las 4 consultas: verifica `tiene_permiso(auth.uid(), 'puedeHacerBackup')` antes de tocar cualquier tabla, y como `SECURITY DEFINER` bypasea RLS, repite el filtro por sede a mano en las 4 sub-consultas (antes lo hacía RLS de forma implícita). Mismo patrón de resolución de sede que `conflictos_horario`/`0067` y `contar_docentes_esperados`/`0072`. Sin fallback al camino inseguro si el RPC no existe todavía (a diferencia de `borrar_horarios` en `clearAllData`) — el mensaje de error es accionable, no un fallback silencioso al mismo hueco que se está cerrando. 6 tests nuevos (reemplazan los 4 de `PERM-7`, que testeaban las consultas directas ya eliminadas). 352/352 tests, 0 errores de lint, build limpio |
| **PROG-1** 🟡 | Arranque de la serie `PROG-N` (análoga a `SEDE-N`): mapeo de los puntos de lectura que dependían de `puedeVerTodo`/`puedeVerSoloSuPrograma` como filtro solo-en-cliente, sin respaldo en RLS. 6 puntos identificados (`HistorialView.jsx`, `PlanillaQR.jsx`, `ReporteAsistencias/*`, `useDataSync.js`). Complicación de diseño: coordinadores pueden tener más de un programa (a diferencia de sede, 1:1) — necesita relación N:N, no columna escalar | `useAuth.js`, `usePerfilEfectivo.js`, `App.jsx`, `HistorialView.jsx`, `PlanillaQR.jsx`, `ReporteAsistencias/*` | — | 🟢 Mapeo completo, superado por `PROG-3` (serie cerrada, ver nota al final del bloque) |
| **PROG-2** 🟢 | Primera fase de implementación: solo esquema, sin RLS todavía (mismo criterio de fases que `SEDE-1`/`0061`). Tabla `user_profiles_programas` (N:N) en vez de columna escalar; `user_profiles.programa` se deja como "programa principal/legado". Helper `usuario_puede_ver_programa()`, mismo patrón que `usuario_puede_ver_sede()` | `docs/supabase/migrations/0078_prog2_user_profiles_programas.sql` | `0078` | ✅ Cerrado — verificado en Supabase real (9 ago) |
| **PROG-3 fase 1** 🟢 | Primer prerrequisito de `PROG-3`: UI en `ModalUsuario.jsx` para asignar más de un programa a un coordinador — hoy era de-uno-solo, escribiendo solo la columna escalar. **Backend nuevo:** RPC `admin_get_user_profiles_programas()` (lista `(user_id, programas[])`, mismo scoping por sede que `admin_get_users()`) y RPC `admin_set_user_programas(p_user_id, p_programas)` (reemplaza el conjunto completo en `user_profiles_programas`, valida contra `restringe_programa` del rol, sincroniza la columna escalar legada con el primer elemento). **Frontend:** `ModalUsuario.jsx` cambia de `<select>` único a lista de checkboxes (`form.programas` como array); `PestanaUsuarios.jsx` carga y mezcla `admin_get_user_profiles_programas()` con fallback silencioso si falla (la tabla y el modal siguen funcionando con el escalar legado si esa RPC no está aplicada todavía). **Flujo de creación** (`api/admin-users.js`, `action: "create"`) acepta `programas` (array) con fallback a `programa` (singular) por compatibilidad; guarda el principal en la columna escalar y registra todos en `user_profiles_programas` vía `service_role` — si ese segundo insert falla no revierte la creación del usuario (se loguea para revisar a mano), mismo criterio de no descartar trabajo válido que ya se usó en `PERM-6`/`OFF-10` | `docs/supabase/migrations/0079_prog3_rpcs_multi_programa.sql`, `api/admin-users.js`, `src/components/usuarios/ModalUsuario.jsx`, `ModalUsuario.css`, `PestanaUsuarios.jsx` | ✅ Cerrado (8 ago) — build y **352/352 tests** verificados (sin cambios necesarios en `PestanaUsuarios.integration.test.jsx`: la llamada nueva cae en el mock catch-all existente). Migración `0079` aplicada y verificada en Supabase real (9 ago): `admin_get_user_profiles_programas()` y `admin_set_user_programas()` confirmadas en `pg_proc` |
| **PROG-3 fase 2** 🟢 | Migración de los 6 puntos de lectura mapeados en `PROG-1` a soporte multi-programa (array, no escalar) | `useAuth.js`, `usePerfilEfectivo.js`, `App.jsx`, `HorariosSidebar.jsx`, `HistorialView.jsx`, `PlanillaQR.jsx`, `ReporteAsistencias/{index,ReporteRango}.jsx` | ✅ Cerrado (8 ago) — `programas_asignados` embebido en el perfil, `calcularPermisos()` deriva `programasRestringidos` (array); `App.jsx` pasa de forzar la selección a un "clamp" (mismo criterio que `SEDE-N`). **Hallazgo no anticipado:** `ReporteAsistencias/index.jsx`/`ReporteRango.jsx` no revisaban `puedeVerSoloSuPrograma` en absoluto — dropdown de libre elección para cualquier usuario; corregido con el mismo clamp. **Límite conocido:** `index.jsx`/`VistaAusentes.jsx` consultan `asistencias_diarias` directo, el clamp es defensa en cliente — cierre real depende de `PROG-3 fase 3` (RLS). 352/352 tests |
| **PROG-3 fase 2a** 🟢 | Hueco puntual encontrado durante la fase 2, más grave que el resto: `reporte_asistencias_rango_agregado` (`0069`) es `SECURITY INVOKER` — RLS de sede ya la acota, pero no existe RLS por programa (eso es fase 3), así que `p_programa` era un filtro decorativo. Un usuario restringido a un programa podía mandar `p_programa = NULL` (o el de otro coordinador) y el "Reporte por Rango" devolvía el agregado de TODOS los programas de su sede mezclados. Mismo patrón que `PROG-1a` | `docs/supabase/migrations/0080_prog3_reporte_rango_valida_programa.sql` | ✅ Cerrado (8 ago) — la función pasa de `LANGUAGE sql` a `plpgsql`: si el rol restringe programa, exige `p_programa` no nulo y lo valida contra `usuario_puede_ver_programa()` (`PROG-2`) antes de correr la agregación; rechaza en vez de devolver datos fuera de alcance. Un usuario sin restricción sigue pudiendo mandar `NULL` sin cambios. Migración `0080` aplicada y verificada en Supabase real (9 ago): `reporte_asistencias_rango_agregado()` confirmada en `pg_proc` |
| **PROG-3 fase 3** 🟢 | Cierre de la serie `PROG-N`: enforcement real en RLS. **Corrección de alcance importante** respecto a como quedó anotado en `PROG-1`/`PROG-3 fase 1` ("horarios/docentes/materias/asistencias_diarias"): al revisar el esquema real para escribir esta migración, `docentes` y `materias` **no tienen columna `programa`** — son catálogos compartidos dentro de una sede (un mismo docente dicta en varios programas), no atados a uno. Restringirlos por programa habría sido incorrecto contra el modelo de datos real. El aislamiento por programa solo aplica a `horarios` (`programa` NOT NULL) y `asistencias_diarias` (`programa` nullable) — las dos únicas tablas donde el dato realmente pertenece a un programa. **Hallazgo adicional, gap latente en `usuario_puede_ver_programa()` (creada en `PROG-2`, 0078):** el helper original solo bypasseaba con `puedeVerTodo`, pero `restringe_programa` (rol) y `puedeVerTodo` (permiso) son campos independientes en `ModalRol.jsx` — nada garantiza que un rol no restringido tenga `puedeVerTodo` marcado. Mientras el helper solo se usaba dentro de un `IF v_restringe THEN` (`PROG-1a`, `PROG-3 fase 2a`) el hueco no se manifestaba; al usarlo directo en RLS para TODAS las filas leídas por CUALQUIER rol, sí — se corrigió para bypassear también con `NOT restringe_programa`, superset estrictamente más seguro, sin quitarle acceso a nadie que ya lo tuviera. **Verificación de los flujos de escritura existentes contra la RLS nueva:** import de Excel (`useUpload.js`) ya tiene manejo de error genérico para rechazos RLS/permiso (`mensajeAmigable()`, `UX-31`) — no requirió cambios, un rechazo por programa fuera de alcance ya se traduce a un mensaje amigable. Edición inline (`horarioEditing.js`, `UX-14`) solo reescribe la columna `clase` de filas ya visibles — sin riesgo de regresión. Restauración de backup (`puedeRestaurarBackup`, típicamente rol sin restricción) sin cambios necesarios | `docs/supabase/migrations/0081_prog3_rls_programa_horarios_asistencias.sql` | ✅ Cerrado (8 ago) — **352/352 tests** sin cambios (migración puramente SQL, sin tocar frontend). Migración `0081` aplicada y verificada en Supabase real (9 ago): `pg_policies` confirma `usuario_puede_ver_programa(programa)` activo en las políticas de `horarios` y `asistencias_diarias` — el enforcement real, no solo en código |

**Serie `PROG-N` cerrada por completo** (`PROG-1` → `PROG-3 fase 3`, 8 ago): mapeo inicial, tabla N:N de asignación multi-programa, UI de gestión, migración de los puntos de lectura del cliente, y enforcement real en RLS. 6 migraciones nuevas (`0077`–`0081`, más `0075` de `OFF-10` que quedó pendiente de la sesión anterior) — **las 6 aplicadas y verificadas en Supabase real el 9 ago** (verificación directa contra el esquema real: tabla, funciones vía `pg_proc`, y política RLS vía `pg_policies`; no contra el historial de `supabase migration list`, que no las registra por haberse aplicado con SQL directo en vez del CLI). El enforcement por programa ya vive en la base real, no solo en el código.
| **PROG-1a** 🟢 | Hueco puntual encontrado durante el mapeo de `PROG-1`: el RPC `exportar_backup_completo` (`0076`, `PERM-6`) ya verificaba `puedeHacerBackup` server-side, pero `p_programa` lo mandaba el cliente sin validar — `backupActions.js` lo llena directo desde `selectedPrograma`, estado cliente-only. Un usuario restringido a un solo programa (rol con `restringe_programa`, sin `puedeVerTodo`) podía forzar `p_programa` a otro valor, o a `null`/`'todos'`, y exportar el backup de un programa que nunca podría ver en la UI normal — mismo patrón de riesgo que `p_sede_id` ya tenía resuelto en el mismo RPC desde `0076` | `docs/supabase/migrations/0077_prog1_backup_valida_programa.sql` | ✅ Cerrado (8 ago) — se agrega `v_programa_efectivo` con el mismo tratamiento que ya tenía `v_sede_efectiva`: si el usuario tiene `puedeVerTodo`, se respeta `p_programa` tal cual manda el cliente; si no, se ignora por completo y se fuerza `user_profiles.programa` (rechaza el backup si ese usuario no tiene programa asignado, en vez de dejarlo pasar sin filtro). No requirió cambios en `backupActions.js` ni en sus 6 tests existentes — el fix es enteramente del RPC, y el cliente sigue mandando lo mismo que mandaba; ahora el servidor decide si lo respeta. Migración `0077` aplicada y verificada en Supabase real (9 ago): `exportar_backup_completo()` confirmada en `pg_proc` |
| **PERM-7** 🟢 | Reabre `PERM-4` (cerrado como "corregido a `asistencias_diarias`" hace semanas, pero el fix nunca llegó al código real): `exportarDatos()` seguía consultando una tabla `asistencias` (sin sufijo) que **nunca existió** en el esquema — confirmado con `grep` contra las 75 migraciones. Encontrado el 8 de ago revisando quién controla `puedeHacerBackup` (candidato a `PERM-6`, ver Historial) para entender el enforcement de permisos server-side vs. solo-UI. Ninguna de las 4 consultas de `Promise.all` revisaba `.error`, así que cada backup exportado hasta ahora quedaba con `asistencias: []` en silencio, igual marcado `asistencias_incluidas: true` — sin ningún aviso visible para quien lo descargó | `src/hooks/useAppData/backupActions.js` | ✅ Cerrado (8 ago) — tabla corregida a `asistencias_diarias`; las 4 consultas ahora revisan `.error` — si cualquiera falla, el backup NO se descarga y se muestra un toast nombrando qué tabla(s) fallaron, en vez de descargar algo incompleto disfrazado de completo. 4 tests nuevos (`backupActions.exportarDatos.test.js`). 315/315 tests, 0 errores de lint, build limpio |

## 📡 Offline y estado de red

Esquema `OFF-N` (antes `O-N`/`P-N`).

| ID | Descripción | Archivo(s) clave | Estado |
|---|---|---|---|
| **OFF-1** | Sin manejo de estado offline/online para renovación del token QR | `src/hooks/useQRSession.js` | ✅ Cerrado |
| **OFF-2** | Registros irrecuperables de la cola offline nunca se purgaban | `useSyncPendientes.js`, `offlineQueue.js` | ✅ Cerrado (TTL 48h) |
| **OFF-3** | Sin indicador visual de red caída en la proyección del aula | `QRProyeccion.jsx` | ✅ Cerrado |
| **OFF-4** | El poll de rotación de QR seguía intentando queries sin conexión | `src/hooks/useQRSession.js` | ✅ Cerrado |
| **OFF-5** | Service Worker no se registraba explícitamente | `src/main.jsx` | ✅ Cerrado |
| **OFF-6** | Lockout de PIN en `localStorage` no resistía pestañas privadas | `LoginScreen.jsx` | ✅ Cerrado (migrado a IndexedDB) |
| **OFF-7** | `DocenteScan` sin manejo offline | `DocenteScan/index.jsx` | ✅ Cerrado (encola en IndexedDB, confirmación optimista) |
| **OFF-8** | Validación de token sin timeout — spinner infinito sin red | `DocenteScan/index.jsx` | ✅ Cerrado (timeout 3s) |
| **OFF-9** 🟢 | Rate limit de `api/csp-report.js` (`SEC-24`) vivía en `Map()` en memoria del proceso — no persiste entre instancias serverless de Vercel, eludible repartiendo requests | `api/csp-report.js`, migración `0060` | ✅ Cerrado (2 ago) — contador movido a tabla `csp_report_rate_limit` vía RPC (mismo patrón que `SEC-16`), fail-open si la RPC falla. Verificado contra Postgres real: 20/21 llamadas → `true`, la 21ª → `false` |
| **OFF-10** 🟢 | `AdminQRPanel` bloqueaba por completo `crearSesion` sin red — el offline previo (`OFF-1`/`OFF-7`) solo cubría continuidad dentro de una sesión ya activa, no el arranque en frío. Contexto: cortes eléctricos de 5+ horas diarios reportados por LS | `useQRSession.js`, `qrOfflineCache.js`, `manualAttendanceQueue.js`, `useSyncPendientes.js`, `AdminQRPanel.jsx`, migración `0075` | ✅ Cerrado (8 ago) — 2 mitades: **(A)** `prepararSesionOffline()` cachea en IndexedDB sesiones de turnos futuros mientras hay red, `crearSesion()` activa localmente si el contexto coincide. **(B)** sin nada cacheado, formulario de registro manual que encola y sincroniza contra RPC `registrar_asistencia_manual` (`0075`, `SECURITY DEFINER`, misma resolución de sede que `crear_qr_session`, sin bypass de permiso). Se descartó generar la sesión 100% client-side por abrir el mismo tipo de bypass que `SEC-28`/`29`. 26 tests nuevos. **Drift resuelto antes de entregar:** implementado sobre un HEAD desactualizado, reconciliado a mano contra 9 commits paralelos de LS (migración renumerada `0071`→`0075`). 346/346 tests tras reconciliar. **Verificado en producción (16 ago):** `registrar_asistencia_manual()` confirmado aplicado en `pg_proc` |
| **OFF-11** 🟢 | `TTL_MINUTES = 5` en `useQRSession.js` acoplaba dos cosas distintas: la cadencia de *rotación* del token (protección anti-foto-compartida) y el margen de *expiración* de respaldo (`expires_at`) que valida `registrar_asistencia` al sincronizar. Con la rotación pausada durante un corte (`OFF-1`), casi todo lo escaneado offline después del minuto ~5 sincronizaba contra un token ya vencido (`TOKEN_EXPIRADO`) | `src/hooks/useQRSession.js` | ✅ Cerrado (8 ago) — desacoplado en `ROTATION_MINUTES = 5` (cadencia de rotación, sin cambios, sigue siendo lo que se muestra en el `CountdownBar` de `QRDisplay.jsx`) y `EXPIRY_TTL_MINUTES = 360` (el `p_ttl_min` real que se manda a `crear_qr_session`/`renovar_qr_token`). Cada rotación exitosa empuja `expires_at` 6h hacia adelante, así que el último token capturado antes de un corte sigue siendo válido al sincronizar horas después, sin afectar la cadencia de rotación visible mientras hay red. 2 tests nuevos en `useQRSession.integration.test.js` verificando el desacople. Depende de `OFF-10` (la pre-generación de sesiones asume este TTL holgado) |
| **OFF-12** 🟡 | A diferencia de asistencia (`OFF-10`/`11`), la carga de horarios por Excel (`useUpload.js`) no tenía ruta de recuperación ante un corte de red a mitad de proceso — mismo contexto operacional de `OFF-10` (cortes eléctricos largos) | `excelUploadQueue.js` (nuevo), `idb.js`, `useUpload.js`, `HorariosTopbar.jsx` | ✅ Cerrado (10 ago) — a diferencia de `OFF-10`, no encola el insert ya resuelto (el catálogo de docentes/materias pudo cambiar mientras tanto) — guarda el **archivo original** + contexto en `cargas_excel_pendientes`, detectado vía `esErrorDeRed()`. Reintento manual (badge en `HorariosTopbar`, sin auto-sync a propósito): re-corre el flujo completo contra el catálogo fresco. TTL 48h. 6 tests nuevos, 411/411 tests, 0 errores de lint, build limpio |
| **UX-36** 🟢 | `esErrorDeRed()` (`OFF-12`) solo reconoce cortes de red — un JWT vencido a mitad de una carga de Excel no pasaba por ahí. Supabase devuelve un JWT vencido como `{ error }` normal del `insert()` (`PGRST301`), no como excepción, y esa rama nunca revisaba si el error era recuperable | `useUpload.js` | — | ✅ Cerrado (11 ago) — helper `esErrorDeSesionExpirada()` + `esErrorRecuperable()` (red O sesión). Mismo mecanismo de cola que `OFF-12`, mensaje de toast distinto según la causa. 2 tests nuevos, 530/530 tests, 0 errores de lint, build limpio |

## 🏗️ Arquitectura, testing y concurrencia

Esquema unificado `ARCH-N` — antes 3 esquemas distintos (`A1`/`A2`/`A3` sin
guion, `A-2`..`A-5` con guion, `ARCH-4`..`ARCH-19`). Ver tabla de
equivalencias al final.

### Concurrencia y datos asíncronos

| ID | Descripción | Archivo(s) clave | Estado |
|---|---|---|---|
| **ARCH-1** | Colisión de nombres entre stores IndexedDB — crasheaba el bundle de producción (TDZ) | `pinOffline.js`, `offlineQueue.js`, `reporteCache.js` | ✅ Cerrado (prefijos únicos) |
| **ARCH-2** | Sin paginación por cursor en `ReporteRango` | `ReporteAsistencias/ReporteRango.jsx` | ✅ Cerrado — el fix original asumía IDs enteros; bug real corregido después en `UX-15` (`asistencias_diarias.id` es UUID) |
| **ARCH-3** | Sin guardia de sanidad si el cursor de paginación no avanza | `useAppData/useDataSync.js` | ✅ Cerrado (retirada al pasar `ARCH-2`/`UX-15` a paginación por offset) |
| **ARCH-4** | Sin `AbortController` — fetches obsoletos podían sobreescribir estado más reciente | `ReporteRango.jsx`, `useQRSession.js` | ✅ Cerrado |
| **ARCH-5** | Sin limpieza de datos al iniciar un fetch sin caché | `ResumenView.jsx`, `useDataSync.js` | ✅ Cerrado |

### Testing, código muerto y estructura de componentes

| ID | Descripción | Archivo(s) clave | Estado |
|---|---|---|---|
| **ARCH-6** | `log_audit_event` sin registrar rol/programa del actor | migración `0025` | ✅ Cerrado |
| **ARCH-7** | Sin cobertura de tests para lógica crítica (`useAuth`, cola offline) | `useAuth.test.js`, `offlineQueue.test.js` | ✅ Cerrado |
| **ARCH-8** | Sin tests de integración para hooks compuestos ni flujos completos (escaneo QR, horarios, usuarios) | `PestanaUsuarios.integration.test.jsx`, `DocenteScan.flow.test.jsx` + 5 tests de orquestación | ✅ Cerrado — 152/152 tests |
| **ARCH-9** | CSS embebido de `QRProyeccion.jsx` con el stylesheet duplicado en el mismo template literal | `asistencias/QRProyeccion.jsx` | ✅ Cerrado (5 jul, junto con `SEC-3`) — extraído a `QRProyeccion.css` |
| **ARCH-10** | Bundle sin dividir por ruta — chunk principal de 514 KB | `vite.config.js`, vistas grandes de `HorariosLayout.jsx` | ✅ Cerrado (9 jul) — `lazy()`+`Suspense` en vistas grandes; `ResumenView` estática a propósito (vista por defecto). 503→468 KB |
| **ARCH-11** | `HorariosLayout.jsx` (561 líneas) y `App.jsx` (353 líneas) concentraban layout, navegación y sesión | `src/app/HorariosLayout.jsx`, `src/App.jsx` | ✅ Cerrado — `HorariosSidebar.jsx`/`HorariosTopbar.jsx` extraídos; 561→293 y 353→338 líneas |
| **ARCH-12** | Código muerto: ningún archivo lo importaba/renderizaba | `src/components/ResponsiveStyles.jsx` | ✅ Cerrado — eliminado |
| **ARCH-13** | `HistorialView.jsx` (637), `LogsView.jsx` (517), `LoginScreen.jsx` (508) — mismo problema que `ARCH-11` | `src/components/{HistorialView,LogsView,LoginScreen}.jsx` | ✅ Cerrado (9 jul noche) — cada uno dividido en orquestador + subcomponentes presentacionales (`historial/`, `login/`, `logs/`). Extracción 1:1 verificada línea por línea |
| **ARCH-14** | `api/admin-users.js` repetía el mismo bloque fetch/headers/parseo 13 veces | `api/admin-users.js` | ✅ Cerrado (11 jul) — extraído `supabaseAdminFetch(path, options)`, 13 llamadas migradas 1:1 |
| **ARCH-15** | Chunk `view-qr` pesaba 320 KB — `vite.config.js` forzaba `AdminQRPanel`/`QRProyeccion`/`ReporteAsistencias` a un único `manualChunks`, anulando su `lazy()` individual | `vite.config.js`, `AdminQRPanel.jsx`, `QRProyeccion.jsx` | ✅ Cerrado (12 jul) — extraído código compartido a `QRDisplay.jsx` (import estático cruzado era la causa real). 3 chunks reales: admin 19 KB, proyección 6.5 KB, reporte 37.8 KB |
| **ARCH-16** | Suite de tests dependía de un tarball externo (`cdn.sheetjs.com`) para `xlsx` sin fallback — fallaba en redes restringidas | `package.json`, `vendor/xlsx-0.20.3.tgz` | ✅ Cerrado (12 jul) — tarball vendorizado con hash SHA-256 documentado en `vendor/README.md` |
| **ARCH-17** | **Más grave que `ARCH-15`**: Rollup metía el cliente de Supabase, logger y utils usados por *toda la app* dentro del chunk `view-qr` — cualquiera que abre el login ya descargaba los 320 KB de QR | `vite.config.js` (`manualChunks`) | ✅ Cerrado (12 jul) — `manualChunks` de forma objeto a función; grafo real de módulos (`getModuleInfo()`) usado para encontrar los 8 módulos a extraer. `vendor-supabase` (214 KB) y `vendor-core` (9 KB) separados. `view-qr` 320→90 KB; también corrigió el mismo problema no documentado en `view-historial` |
| **ARCH-18** | `AdminQRPanel.jsx` volvió a crecer a 685 líneas | `AdminQRPanel.jsx`, `adminQR/HistorialSesiones.jsx`, `adminQR/ConfirmBorrarSesionModal.jsx` | ✅ Cerrado — mismo patrón que `ARCH-11`/`ARCH-13`: 685→543 líneas |
| **ARCH-19** | Sin ESLint ni Prettier configurados, sin paso de lint en CI | `eslint.config.mjs`, `package.json`, `ci.yml` | ✅ Cerrado (12 jul) — flat config, 3 plugins mínimos (`@eslint/js`, `react-hooks` solo 2 reglas, `react-refresh`). 31 errores reales corregidos (código muerto/imports), 33 warnings no bloqueantes quedan a propósito. `npm run lint` bloqueante en CI. 2 inconsistencias de comportamiento encontradas en la limpieza **no se tocaron** aquí — derivaron en `ARCH-22` y `UX-14` |
| **ARCH-20** | Cero uso de PropTypes/TypeScript | 8 componentes más reutilizados (`QRDisplay`, `Avatar`, `ModalUsuario`, etc.) | ✅ Cerrado — `propTypes` agregado, cada `shape` verificado contra call sites reales, no adivinado |
| **ARCH-21** | Chunk principal (446 KB) pesado incluso solo para ver el login | `useAppData/useUpload.js`, `vite.config.js` | ✅ Cerrado (13 jul) — medido con `rollup-plugin-visualizer`: culpable real era `xlsx`/SheetJS (750 KB) importado estático, no `@tabler/icons-webfont` como se sospechaba. Cambiado a `import()` dinámico en los 2 puntos de uso real. 447→74 KB (-83%) |
| **ARCH-22** | `UploadPreviewModal.jsx`: el toggle "mostrar X más" no hacía nada — la agrupación real usaba `rows` completo, no `visible` (no es bug de linting, por eso no se tocó junto a `ARCH-19`) | `UploadPreviewModal.jsx`, `.css` | ✅ Cerrado (13 jul) — decisión de LS: retirado `expanded`/`hasMore`/`visible` y el botón muerto, en vez de arreglar un límite que nunca se pidió |
| **ARCH-23** | `DocenteScan/index.jsx` a 525 líneas — mismo problema ya corregido 3 veces antes, nunca aplicado aquí | `asistencias/DocenteScan/index.jsx` | ✅ Cerrado (15 jul) — dividido en orquestador (342 líneas) + `PasoValidacionCedula.jsx` + `PasoRegistro.jsx`, mismo patrón que `ARCH-13`/`ARCH-18` |
| **ARCH-24** | Chunk principal creció de 74.47 KB a 134.61 KB desde `ARCH-21`, sin que nadie lo revisara tras `ADMIN-3/4/5` | `vite.config.js`, `src/App.jsx` | ✅ Cerrado (15 jul) — corrección de premisa: ninguno de los 3 módulos raíz era `lazy()` (no solo `AdminModulo` como decía el hallazgo). `AsistenciasModulo`/`AdminModulo` convertidos a `lazy()`; `HorariosLayout` estático a propósito (entrada por defecto). 134.75→128.76 KB (~4.4%) |
| **ARCH-25** | `ReporteRango.jsx` (el mismo componente del bug real `UX-15`), `AdminModulo.jsx`/`ModuleSelector.jsx` (deciden qué módulos ve cada rol) sin tests dedicados | `ReporteRango.jsx`, `AdminModulo.jsx`, `ModuleSelector.jsx` | ✅ Cerrado (15 jul) — 18 tests de integración nuevos (render real con `@testing-library/react`) |
| **ARCH-26** | `VistaAusentes.jsx` pedía siempre el catálogo completo de `docentes` como fallback de resolución de nombre, aunque el 100% de las clases ya tuvieran `docente_id` vinculado (el fallback es el caso raro, no el común) | `ReporteAsistencias/VistaAusentes.jsx` | ✅ Cerrado (1 ago) — el catálogo solo se pide si `clases.some(c => !c.docente_id)`. 3 tests de integración nuevos cubriendo ambas ramas (con/sin fallback) y el filtrado de presentes |
| **ARCH-27** | `ReporteRango.jsx` paginaba hasta 20.000 filas crudas de `asistencias_diarias` (bloques de 1000) y agrupaba por docente en el cliente — mucho tráfico de red y varias idas y vueltas sin feedback de progreso para el usuario | `ReporteRango.jsx`, migración `0055` (RPC `reporte_asistencias_rango_agregado`) | ✅ Cerrado (1 ago) — agregación `GROUP BY cedula_docente` server-side (`SECURITY INVOKER`, respeta el RLS de `SEC-11` sin cambios de superficie de seguridad). Elimina también el límite de 20.000 filas y el aviso de "resultado truncado" (ya no aplica: no se transfieren filas individuales). Conteo liviano (`head: true`) agregado aparte solo para el texto del modal de borrado, que sigue operando sobre registros individuales. **Nota:** el índice de auditoría ya referenciaba una migración "0055" (pg_cron de `SEC-21`) sin archivo en el repo — verificar contra la BD real antes de aplicar, ver comentario en la migración |
| **ARCH-28** | `filtrados`/`diasHabiles` (`ReporteRango.jsx`) y `filtrados` (`ReporteAsistencias/index.jsx`) se recalculaban en cada render sin memoizar, aunque sus dependencias reales no cambiaran | `ReporteAsistencias/index.jsx`, `ReporteRango.jsx` | ✅ Cerrado (1 ago) — `useMemo` con las dependencias reales en los 3 casos |

| **ARCH-30** 🟡 | `HorariosLayout.jsx` (shell de los módulos resumen/horarios/secciones/docentes/materias/asistencias) no tenía `ErrorBoundary` propio — un crash de render en cualquiera de esas vistas escalaba hasta el `ErrorBoundary` global de `main.jsx`, tumbando toda la app (sidebar y topbar incluidos) en vez de quedar contenido en el área de contenido | `src/app/HorariosLayout.jsx` | ✅ Cerrado (2 ago) — mismo patrón ya usado en `AsistenciasModulo.jsx`/`AdminModulo.jsx`: `ErrorBoundary` local envolviendo el `<main className="hl-main">`, no el layout completo. Sidebar/topbar siguen operativos aunque una vista crashee. 211/211 tests preexistentes sin romper, 0 errores de lint, build limpio |
| **ARCH-31** 🔴 | Reabre `ARCH-29`/`UX-24`: el bloqueo optimista de `saveClase()` (`horarioEditing.js`) condiciona el `UPDATE` a `.eq("updated_at", expectedUpdatedAt)`, pero la migración `0057` que ese fix citaba (columna `updated_at` + trigger server-side en `horarios`) nunca se había subido al repo — `entry.updated_at` llegaba `undefined` en producción y el guard se degradaba siempre a "sin condición extra", el mismo comportamiento inseguro que `ARCH-29` describía como bloqueante | `docs/supabase/migrations/0057_arch29_bloqueo_optimista_horarios.sql` (nueva) | ✅ Cerrado (2 ago) — migración subida: `ALTER TABLE horarios ADD COLUMN updated_at` + función/trigger `set_updated_at()` `BEFORE UPDATE` sobre el padre (Postgres 11+ propaga triggers `FOR EACH ROW` de una tabla particionada declarativa a todas sus particiones automáticamente, sin loop). Verificado contra la BD real, no asumido: `SELECT tgrelid::regclass, tgname FROM pg_trigger WHERE tgname = 'trg_horarios_set_updated_at'` devuelve 8 filas — el padre `horarios` + las 7 particiones reales (`horarios_lapso_1_2026`...`horarios_lapso_default`). Confirmado también que `useDataSync.js` usa `select("*, ...")` en el fetch que puebla `ModalEditarClase`, así que `entry.updated_at` llega poblado al cliente. 211/211 tests, 0 errores de lint, build limpio |
| **ARCH-32** 🟢 | `registrar_asistencia()` (`SEC-13`/migración `0039`) usa un límite fijo (10 intentos/hora por `device_fingerprint`) sin backoff exponencial — un dispositivo legítimo con mala señal que reintenta agresivamente en un operativo real puede auto-bloquearse antes de llegar al límite por abuso genuino | `scan_rate_limit`, RPC `registrar_asistencia`, migración `0058` | ✅ Cerrado (2 ago) — backoff progresivo: la primera vez que un dispositivo supera el límite recibe un bloqueo corto (2 min); si reincide después de que ese bloqueo expira, el bloqueo se duplica (4, 8, 16... techo de 60 min). `veces_bloqueado` decae a 0 tras 24h sin bloqueos nuevos. **No se pudo probar contra Supabase real desde este entorno** (mismo patrón que `SEC-9`/`SEC-17`) — migración incluye escenario de verificación manual |
| **ARCH-33** 🔴 | Migración `0058` (de `ARCH-32`) reemplazó el UPSERT atómico de `0039` por `SELECT FOR UPDATE`+`INSERT` suelto en `registrar_asistencia()` — condición de carrera real: 2 llamadas concurrentes del mismo `device_fingerprint` sin fila previa chocan por PK | RPC `registrar_asistencia`, migración `0059` | ✅ Cerrado (2 ago) — reproducido contra Postgres 16 real (`duplicate key value violates unique constraint "scan_rate_limit_pkey"`), corregido con `INSERT ... ON CONFLICT DO UPDATE` (vuelve al patrón de `0039`). Reverificado bajo concurrencia real: 15 llamadas → 5 bloqueadas, escalada de `veces_bloqueado` correcta |
| **ARCH-34** 🟢 | Sin guardia de CI que impidiera reintroducir herramientas de build-time (causa raíz de `SEC-26`) como `dependencies` en vez de `devDependencies` | `.github/workflows/ci.yml` | 🔴 Reabierto como `ARCH-35` (3 ago) — se había marcado ✅ cerrado sin que el guard llegara realmente al `ci.yml` del repo |
| **ARCH-35** 🟢 | Reabre `ARCH-34`: mismo patrón que `ARCH-29`→`ARCH-31` (hallazgo cerrado en el índice, nunca aplicado al código real) — verificado esta vez contra el archivo, no solo el índice | `.github/workflows/ci.yml` | ✅ Cerrado (3 ago) — guard reaplicado y confirmado presente en el archivo real; `node -e` de verificación corrido contra el `package.json` actual: pasa |
| **ARCH-36** 🔴 | Las migraciones `0066`/`0067` vivían en `supabase/migrations/`, una carpeta distinta a la secuencia real (`docs/supabase/migrations/`), sin `config.toml` de Supabase CLI ni referencia en ningún script de CI — riesgo de que se aplicaran fuera de orden o nunca (mismo patrón de fondo que `ARCH-29`/`34`) | `supabase/migrations/`, `.github/workflows/ci.yml` | — | ✅ Cerrado (6 ago) — `0066`/`0067` movidas a `docs/supabase/migrations/` y la carpeta huérfana `supabase/` eliminada (commits `72985d7`, `1ca22cd`, `07042f4`), más guard nuevo en CI que falla el build si vuelve a aparecer un `.sql` de migración fuera de `docs/supabase/migrations/`. Probado contra el estado real del repo: pasa limpio |
| **ARCH-37** 🔴 | El sistema de Sedes completo (`SEDE-1`–`14`, migraciones `0061`–`0068`, ~25 archivos de frontend con `sede_id`) nunca se registró en este índice, violando la metodología que el propio documento se auto-impone — causa raíz por la que `SEC-28`/`SEC-29`/`ARCH-36` pasaron sin auditar | `docs/AUDITORIA_INDICE.md` | — | ✅ Cerrado (6 ago) — nueva sección `§ Sistema multi-sede (SEDE-N)` documenta las 14 pasadas; auditadas también las ~50 funciones `SECURITY DEFINER` únicas definidas en el histórico de migraciones para confirmar que `SEC-28`/`SEC-29`/`SEDE-11`/`12`/`14` cubren todas las que tocaban tablas con `sede_id` sin filtro |
| **ARCH-38** 🟢 | Recurrencia del patrón `UX-27`: el fix `fecha-hoy-timezone` (`fechaHoyVE()`, junio) nunca tuvo una suite dedicada que fijara el tramo exacto 20:00–00:00 hora VE (la ventana real del bug original — UTC ya rueda al día siguiente mientras VE no) — el riesgo de recurrencia silenciosa quedaba solo cubierto de forma indirecta por `UX-27`/`UX-33` | `src/utils/cruceMedianocheVE.test.js` (nuevo) | ✅ Cerrado (9 ago) — 22 tests nuevos: `fechaHoyVE()` en el minuto exacto del cruce (20:00, 23:59:59, 00:00:00, 00:00:01 VE) y recorrido continuo 19:00→01:00 VE; `diaSemanaVE()` combinada con `fechaHoyVE()` en el mismo cruce; round-trip `timeToMin`/`minToTime` en los extremos 0/1439; y comportamiento **actual** (no ideal) de `parseRango`/`countBlocks`/`solapan` (`conflictos.js`, `time.js`) para un rango que cruza medianoche real (ej. `"8:00PM - 12:00AM"`) — documentado como hallazgo menor sin explotar hoy: ningún turno de `TURNOS_CONFIG` cruza medianoche (`NOCTURNO` llega solo hasta 9:30pm y está deshabilitado), y `parseRango` no distingue una hora de inicio literal de medianoche (`timeToMin` devuelve `0` tanto para `"12:00AM"` como para una hora no reconocible) — queda anotado en el propio test para no perderse si se habilita un turno nocturno extendido en el futuro. 374/374 tests, 0 errores de lint, build limpio |
| **ARCH-39** 🟢 | Pendiente L: sin test de carga repetible que reprodujera el escenario real de `ARCH-33` (condición de carrera en `scan_rate_limit` bajo llamadas concurrentes del mismo dispositivo) — ese hallazgo se probó a mano una única vez contra Postgres real y nunca quedó como regresión en CI; nada impide que una migración futura reintroduzca el mismo patrón (`SELECT`-then-`INSERT` en vez de `UPSERT` atómico) sin que nada lo note. El smoke-test de RLS con clave anon que pedía el mismo pendiente **ya existía** (`CI-3`, `scripts/rls-smoke-test.mjs`, bloqueante en CI desde antes) — no se tocó | `scripts/qr-load-test.mjs`, `.github/workflows/qr-load-test.yml` (workflow separado desde `ARCH-43`, 10 ago — ya no vive en `ci.yml`) | ✅ Cerrado (16 ago) — **primera corrida real con los 3 secrets presentes (log subido por LS), test ejecutado de punta a punta:** oleada throughput, 40/40 docentes distintos exitosos, 0 rechazos, 0 errores inesperados, p50=507ms/p95=530ms; oleada rate-limit-concurrente, 15 llamadas del mismo dispositivo → 1 exitosa, 5 rechazadas con `RATE_LIMIT` (esperado), 9 con `DEVICE_DUPLICADO`, 0 errores inesperados. **`DEVICE_DUPLICADO` explicado, no es una falla:** el fixture de esta oleada usa 15 cédulas *distintas* sobre el mismo `device_fingerprint` — el guard (e) de `registrar_asistencia()` ("mismo dispositivo ya usado por otra cédula en esta sesión") intercepta la mayoría de las llamadas concurrentes en cuanto la primera cédula commitea, antes incluso de llegar al contador de `MAX_INTENTOS`; es un guard antifraude distinto y anterior al rate limit, no un fallo de éste. Lo que valida `ARCH-33` (sin error crudo de Postgres bajo el `UPSERT` concurrente de `scan_rate_limit`) y que el rate limit sí rechaza bajo concurrencia real (≥1 `RATE_LIMIT`) — ambas condiciones se cumplieron. **Corrección sobre esta misma fila:** una entrada anterior decía que el job usaba `continue-on-error: true` pendiente de "graduar a bloqueante" — verificado contra `.github/workflows/qr-load-test.yml` real, ese flag nunca estuvo en este workflow (separado de `ci.yml` desde `ARCH-43`); el job ya falla por sí solo (`process.exit(1)` en el script) si `fallas.length > 0`, sin nada que graduar. Simula el escenario real de `DocenteScan` contra `registrar_asistencia()` con la clave **anónima** (igual que un teléfono real), preparando/limpiando el fixture (sesión QR canario) con la clave de **servicio**. Limpieza automática de todas las filas canario (`asistencias_diarias`/`scan_rate_limit`/`qr_sessions`) en un `finally`, confirmada en el log ("Limpieza completada"). Corre semanal (cron lunes 6:30 UTC) + bajo demanda desde Actions |

| **ARCH-40** 🟡 | Encontrado en la auditoría completa de BD (9 ago, bloat/autovacuum): `scan_rate_limit`, `admin_actions_rate_limit`, `csp_report_rate_limit` son tablas de upsert por clave (misma fila se actualiza repetidamente) con muy pocas filas vivas en producción hoy (1-2) — `autovacuum` dispara con `umbral + 20% × filas_vivas`, así que ese 20% casi nunca se alcanza sin importar cuántos `UPDATE` se acumulen. Confirmado con `pg_stat_user_tables`: las 3 tienen `autovacuum_count=0`/`autoanalyze_count=0` pese a hasta 92% de filas muertas (`scan_rate_limit`: 1 viva / 11 muertas). No explotable hoy por el volumen bajo, pero es bloat invisible que no se nota por conteo de filas y va a degradar con tráfico real (picos de reintentos de escaneo ya documentados en `OFF-10`) | `scan_rate_limit`, `admin_actions_rate_limit`, `csp_report_rate_limit` | `0083` | ✅ Cerrado (9 ago) — `autovacuum_vacuum_scale_factor=0.02`, `autovacuum_vacuum_threshold=25` por tabla, vía `ALTER TABLE ... SET (...)` (no es un cambio global de `postgresql.conf`) |

| **ARCH-41** 🟡 | Planteado por LS (9 ago): el selector de "Trimestre" en la pestaña Planilla del módulo Asistencias QR usaba `getLapsosDisponibles()` (`utils/lapso.js`), que calcula 5 lapsos por aritmética de fechas (2 anteriores + actual + 2 siguientes desde hoy) **sin consultar la tabla `trimestres`** — ofrecía trimestres futuros aún no configurados y trimestres pasados sin ninguna fila/dato real (ej. 2025), y podía excluir uno real si quedaba fuera del rango ±2. La tabla `trimestres` ya tenía el modelo correcto (`estado`: activo/cerrado, `fecha_inicio`/`fecha_fin`) y `HistorialView.jsx` ya implementa la creación explícita del siguiente trimestre al cerrar el actual (`handleCerrar`→`handleCrear`) — el hueco era solo este selector, no el modelo de datos | `src/components/asistencias/PlanillaQR.jsx`, `src/utils/lapso.js` | ✅ Cerrado (9 ago) — selector reemplazado por consulta real a `trimestres` filtrando `estado IN ('activo','cerrado')`, ordenado por año/número descendente; si el lapso inicial (calculado por fecha) no tiene fila real, se corrige al `activo` o al más reciente disponible; fallback defensivo al trimestre calculado por fecha si la tabla no responde o está vacía. `getLapsosDisponibles()` se conserva solo para cálculos orientativos internos, documentado en su JSDoc que no debe alimentar selectores de UI. 386/386 tests, 0 errores de lint |

| **ARCH-42** 🟢 | Cierra el pendiente de `ARCH-19` (12 jul): 24 warnings de ESLint preexistentes (0 errores), detectados al verificar `ARCH-41` — 19 `react-refresh/only-export-components` (Grupo 1, cosmético: solo afecta Fast Refresh en dev) + 5 `react-hooks/exhaustive-deps` (Grupo 2, lógica real de hooks) | `DocentesView.jsx`, `MateriasView.jsx`, `AdminQRPanel.jsx`, `LoginScreen.jsx`, `DocenteScan/icons.jsx`, `QRDisplay.jsx`, `historialUtils.jsx`, `logsUtils.jsx`, `usuarios/shared.jsx`, `AppDataContext.jsx`, `SedeContext.jsx` | ✅ Cerrado (9 ago) — **Grupo 2** (fixes reales, no solo silenciados): `selEntries` envuelto en `useMemo` propio en `DocentesView.jsx`/`MateriasView.jsx` (la expresión con fallback `\|\| []` creaba un array nuevo en cada render sin selección, invalidando los `useMemo` dependientes sin necesidad); `cargar()` envuelto en `useCallback([fecha])` en `AdminQRPanel.jsx` (agregar la dep sin memoizar primero habría re-disparado el efecto en cada render); `flashTrigger` agregado a las deps del feed en tiempo real de `AdminQRPanel.jsx` (seguro: ya viene memoizado con `useCallback([])` desde `useFlashFeed`); en `LoginScreen.jsx` se confirmó que el input de email está `disabled` mientras `isLocked` (`LoginFormNormal.jsx`) — `email` no puede cambiar durante la vida de ese efecto, así que se documentó con `eslint-disable` en vez de agregarlo sin efecto real. **Grupo 1**: no se separaron los 7 archivos en módulos aparte — tienen hasta 10+ importadores cada uno (`AppDataContext`/`SedeContext`), y dividirlos habría significado tocar ~40 puntos de import en todo el repo por un problema que no afecta producción ni comportamiento, solo la experiencia de Fast Refresh en desarrollo local. Se optó por la alternativa que el propio informe ya contemplaba: `eslint-disable react-refresh/only-export-components` puntual por archivo, con comentario explicando por qué la mezcla es intencional y estable (patrón estándar de Provider+hook en los 2 Context). `npx eslint .` → 24→**0 warnings, 0 errores**. 386/386 tests, build limpio |

| **ARCH-43** 🟡 | Auditoría de estrés operacional (10 ago, simulación de concurrencia masiva): `ARCH-39` (9 ago) ya había creado `scripts/qr-load-test.mjs` y documentó un job `qr-load-test` en `ci.yml` como "pendiente de LS confirmar" — pero, verificado contra el archivo real, ese job **nunca llegó a `ci.yml`**. Mismo patrón exacto ya visto 2 veces antes en este índice (`ARCH-29`→`31`, `ARCH-34`→`35`): un hallazgo documentado como resuelto sin que el fix realmente aterrizara en el código. Sin ese job, una regresión futura del mismo patrón `SELECT`-then-`INSERT` que causó la condición de carrera real de `ARCH-33` podría reintroducirse sin que nada lo note antes de producción | `.github/workflows/qr-load-test.yml` (nuevo) | ✅ Cerrado (10 ago) — a diferencia de lo que `ARCH-39` planteaba (job dentro de `ci.yml`, en cada push a `main`), se optó por un workflow separado (mismo criterio que `SEC-20`/CodeQL): cron semanal (lunes) + `workflow_dispatch` manual, sin bloquear PRs ni pushes normales — el load test le pega a Supabase real y no debe formar parte del feedback rápido de cada cambio. Se salta con exit 0 si faltan `SUPABASE_URL`/`SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY` (mismo comportamiento que `rls-smoke-test.mjs`, ya verificado en el propio script desde `ARCH-39`). YAML validado con `pyyaml`. No requiere tests propios (mismo criterio que `ARCH-39`/`rls-smoke-test.mjs`: es infraestructura de CI, no lógica de aplicación). 411/411 tests preexistentes sin cambios, 0 errores de lint, build limpio. **Pendiente de LS** (hereda el mismo pendiente que `ARCH-39` ya había dejado sin resolver): agregar el secret `SUPABASE_SERVICE_ROLE_KEY` en GitHub → Settings → Secrets, y confirmar la primera corrida real (manual, vía `workflow_dispatch`, no hace falta esperar al próximo lunes) |

## 🔧 CI/CD y automatización

Esquema `CI-N` (antes `FIX-CI-N`).

| ID | Descripción | Archivo(s) clave | Estado |
|---|---|---|---|
| **CI-1** *(no confirmado en código)* | Sin integración continua | `.github/workflows/ci.yml` | ✅ Cerrado (`npm test` + `npm run build` en cada PR/push a `main`) |
| **CI-2** | `console.log/warn/error` directos visibles en producción | `src/utils/logger.js` (14 archivos migrados) | ✅ Cerrado |
| **CI-3** | Sin `npm audit` en CI ni verificación automatizada de RLS con clave `anon` real | `.github/workflows/ci.yml`, `scripts/rls-smoke-test.mjs` | ✅ Cerrado (audit no bloqueante por `SEC-14`; smoke test bloqueante) |
| **CI-4** | 2 usos de `console.info` directo rompían la consistencia del logger | `src/main.jsx`, `src/utils/cache.js` | ✅ Cerrado (9 jul) — `logger.info()` agregado, cero `console.*` fuera de `logger.js` |

## 🎨 UI y estilos

Esquema `UX-N` (antes `U-N` + `A3` de inline styles).

| ID | Descripción | Archivo(s) clave | Estado |
|---|---|---|---|
| **UX-1** | Estilos inline en `AdminQRPanel` — primer caso migrado, sentó el patrón de `UX-5` | `AdminQRPanel.jsx`/`.css` | ✅ Cerrado |
| **UX-2** | Desbordes de layout en viewports móviles pequeños | `AdminQRPanel.css`, `usuarios/ModalRol.jsx` | ✅ Cerrado |
| **UX-3** | Sin trampa de foco de teclado en modales | `src/hooks/useFocusTrap.js` | ✅ Cerrado |
| **UX-4** | `Campo.jsx` sin `htmlFor`/`id` — lector de pantalla no anunciaba la etiqueta | `asistencias/DocenteScan/Campo.jsx` | ✅ Cerrado (`useId()` + `aria-describedby`/`aria-invalid`) |
| **UX-5** | Migración sistemática de estilos inline a CSS externo (requisito de `SEC-3`) | Todo `src/` | ✅ Cerrado — 54→0 ocurrencias reales. `Avatar.jsx` (tono bucketizado a 24 pasos), `TurnoGrid.jsx` (`flex: 1`), `ModalRol.jsx` (10 presets) |
| **UX-6** | Los 7 archivos del shell principal (`src/app/`) nunca se auditaron para responsividad | `HorariosLayout.jsx`, `UserMenu.jsx`, `AsistenciasModulo.jsx`, `App.jsx`, `AdminMenu.jsx`, etc. | ✅ Cerrado — clases con prefijo (`hl-`, `um-`, `asm-`, `adm-`) + `@media` |
| **UX-7** | El bundle sin dividir (`ARCH-10`) alargaba la pantalla en blanco inicial | mismo que `ARCH-10` | ✅ Cerrado (mismo fix) |
| **UX-8** | `LoginFormNormal.jsx`, `LoginOfflinePinPanel.jsx`, `ModalActivarPIN.jsx` (extraídos al cerrar `ARCH-13`): `<label>` sin `htmlFor`/`id` — regresión de `UX-4` | `src/components/login/*.jsx` | ✅ Cerrado (11 jul) — mismo patrón `useId()` que `Campo.jsx` |
| **UX-9** | Solo 4/29 CSS con media queries; `HorariosView.css`/`QRProyeccion.css` sin ninguna | `HorariosView.css`, `QRProyeccion.css` | ✅ Cerrado (12 jul) — falso positivo parcial en la mitad de `QRProyeccion.css` (el responsive real ya vivía en `index.css`, luego movido en `UX-12`); `HorariosView.css` sí carecía de adaptación en la barra de filtros — `@media (max-width: 640px)` agregado |
| **UX-10** | "Panel QR" aparecía con fondo azul oscuro y título invisible — parecía regresión de un fix reciente | `AdminQRPanel.jsx`/`.css`, `QRProyeccion.jsx`/`.css` | ✅ Cerrado (12 jul) — colisión de nombres de clase preexistente (`.qrp-root` etc. duplicado entre 2 CSS con temas incompatibles, agrupados en el mismo chunk Vite). `AdminQRPanel` renombrado a prefijo `qap-` |
| **UX-11** | 24/30 archivos CSS sin `@media` — solo cobertura manual/heurística, sin regresión visual automatizada | 24 `.css`, `ci.yml`, `playwright.config.js`, `tests/visual/` | ✅ **Cerrado (16 jul)** — infraestructura Playwright (3 pantallas × 3 breakpoints) venía en `9 passed` desde el 13 jul; se confirmaron **7 corridas reales más en CI** (15–16 jul, distintos commits) todas `success`, sin ningún diff falso. Retirado `continue-on-error: true` de `ci.yml` — el job ahora bloquea el check si una imagen difiere de la base. Mock de sesión en `tests/visual/mockSupabase.js` |
| **UX-12** | Deuda cosmética de `UX-9`: reglas responsive de `.qrp-*` vivían en `index.css` en vez de `QRProyeccion.css` | `src/index.css`, `QRProyeccion.css` | ✅ Cerrado (13 jul) — ~44 líneas movidas tal cual, sin cambiar reglas |
| **UX-13** ⛔ | Sin `prefers-color-scheme` (modo oscuro) — preferencia de producto, no defecto | tokens `--color-*`/`--brand-*`, 9 `.css` de componentes | ⛔ **Revertido a pedido explícito de LS (14 jul)** — reportó menús/reportes rotos en asistencias, confirmó "no la veo necesaria". Reverso total salvo excepción quirúrgica en `ModuleSelector.css` (preservó el rediseño de `ADMIN-5`, solo revirtió el hunk del modo oscuro). Si se retoma: rehacer desde cero con verificación visual real en navegador |
| **UX-14** | `HorariosView.jsx` recibía `modoConsulta` pero no la usaba — el permiso `puedeEditarHorarios` no tenía funcionalidad real detrás (no existía edición in-line) | `HorariosView.jsx`, `TurnoGrid.jsx`, `ModalEditarClase.jsx` (nuevo), `horarioEditing.js` (nuevo) | ✅ Cerrado (15 jul) — specs de LS: edición por formulario modal (no drag-and-drop), día/bloque/aula/docente/materia + eliminar, con confirmación. Reescribe también la columna `clase` (texto crudo) para no desincronizar 6 pantallas que la leen directo sin pasar por el join. Gating de permisos separado del banner: `puedeEditar`/`puedeBorrar` independientes |
| **UX-15** | Reportado por LS: "Reporte por Rango" tiraba `invalid input syntax for type uuid: "0"` | `ReporteAsistencias/ReporteRango.jsx` | ✅ Cerrado (14 jul) — causa raíz: `asistencias_diarias.id` es UUID, no INTEGER como asumía la paginación por cursor de `ARCH-2`. Cambiado a paginación por offset (`.range()`, ordenado por `hora_registro`) |
| **UX-16** | Reportado por LS: reportes PDF se abrían sin ningún formato (texto plano) | `exportPDF.js`, `public/reporte-print.{css,js}` (nuevos) | ✅ Cerrado (14 jul) — causa raíz: la ventana de impresión (`document.write`) hereda la CSP `'self'` del documento que la abre, bloqueando en silencio el `<style>`/`<script>` inline. CSS y JS extraídos a archivos externos del mismo origen. **Sin verificar visualmente en navegador real** — recomendado que LS confirme abriendo un PDF de cada tipo |
| **UX-17** | Manifest PWA (`theme_color`/`background_color`) fijo en modo claro; en SO con modo oscuro la barra de estado/splash puede no combinar | `vite.config.js` (manifest) | ✅ **Cerrado (16 jul) — sin acción de código, misma decisión que `UX-13`**: la app es 100% de tema claro por decisión de producto, así que no hay "variante oscura" que el manifest deba reflejar. El manifest ya es correcto para el único tema que existe (`#1E3A8A`/`#ffffff`, coinciden con la marca). Reabre solo si se retoma `UX-13` |
| **UX-18** | `ModuleSelector.css` con comentario obsoleto sobre el mecanismo `prefers-color-scheme` de `UX-13` (revertido) | `ModuleSelector.css` | ✅ **Cerrado (16 jul)** — comentario reescrito: ya no cita el mecanismo concreto (removido de `index.css` con el reverso), explica en cambio por qué se usa `--navy-900` en vez de `--color-text-primary` (splash de marca fijo, no debe depender de ningún tema futuro) |
| **UX-19** | "Cambiar módulo" era botón visible en Asistencias/Admin pero enterrado en dropdown en Horarios | `HorariosTopbar.jsx`, `UserMenu.jsx`, `AsistenciasModulo.jsx`, `AdminModulo.jsx` | ✅ Cerrado (14 jul) — unificado hacia botón "← Módulos" en topbar de los 3 módulos, clase compartida `.topbar-back-btn`. Llevó a revisar el sidebar de Horarios — ver `UX-20` |
| **UX-20** | Sidebar de Horarios con solo 5 ítems tras `ADMIN-3`, se sentía subutilizado; código muerto encontrado: `hasBadge` nunca se activaba | `buildNavGroups.js`, `HorariosSidebar.jsx` | ✅ Cerrado (14 jul) — se descartaron 2 propuestas (mini-panel, conteo por ítem) por duplicar info ya visible en otro lado; único cambio real: reactivar `hasBadge: true` en "Horarios" |
| **UX-21** | Estado de conexión solo visible dentro de un dropdown, a diferencia de la caja de trimestre siempre visible | `HorariosSidebar.jsx`, `AdminMenu.jsx`, `index.css` | ✅ Cerrado (14 jul) — `.hl-status-box` nueva en sidebar (mismo trato que `hl-lapso-box`), quitado el bloque equivalente del dropdown |
| **UX-22** | Reportado por LS desde móvil: dropdown de Administración no cerraba al tocar el botón de nuevo (condición de carrera con el listener de "clic afuera") | `AdminMenu.jsx` | ✅ Cerrado (14 jul) — listener ignora clics sobre `.hl-admin-btn`, el botón es la única fuente de verdad de su toggle |
| **UX-23** | Reportado por LS: contador de permisos del admin mostraba "17/15" | `usuarios/shared.jsx` | ✅ Cerrado (15 jul) — `GRUPOS_PERMISOS` (catálogo de UI) le faltaban `puedeBorrarSesiones`/`puedeBorrarReportes`, ya funcionales en código pero sin checkbox en el editor de roles |
| **UX-25** 🟢 | `DocenteScan` encola registros en IndexedDB cuando no hay red (`OFF-7`), pero no muestra un contador visible de cuántos quedan pendientes de sincronizar — un operador de QR en un operativo real con conexión inestable y fila de docentes no tiene forma de saber si sus escaneos ya se confirmaron contra el servidor | `asistencias/DocenteScan/index.jsx`, `Shell.jsx`, `utils/offlineQueue.js` | ✅ Cerrado (2 ago) — badge `role="status" aria-live="polite"` agregado en `Shell.jsx` (el único wrapper común a las ~10 pantallas de `DocenteScan`, evita enhebrar la prop por cada call-site). Autocontenido: lee `contarPendientes()` al montar y se refresca sin polling escuchando un evento DOM nuevo (`sigma:cola-offline-cambio`) que `offlineQueue.js` dispara en `encolarAsistencia()`/`eliminarPendiente()`/`purgarExpirados()`, más `online`/`offline` como respaldo. 8 tests nuevos (5 de integración sobre `Shell`, 3 de regresión sobre el evento). 219/219 tests, 0 errores de lint, build limpio |
| **UX-26** 🟢 | `useDataSync.js`: `useEffect` de la suscripción realtime sin `userId`/`setConflictsRefreshKey` en deps — un cambio de usuario dejaba el closure de `limpiarCache(userId)`/`limpiarCacheNombres(userId)` con el id viejo | `src/hooks/useAppData/useDataSync.js` | ✅ Cerrado (2 ago) — deps agregadas (`setConflictsRefreshKey` es un setter de `useState`, estable). 0 warnings nuevos de `exhaustive-deps` en ese efecto |
| **UX-27** 🔴 | Recurrencia del bug `fecha-hoy-timezone` (ver `utils/time.js`): `ReporteRango.jsx` calculaba el "lunes de esta semana" con `new Date()` crudo (UTC en producción) mientras `fin` ya usaba `fechaHoyVE()` — entre 8pm y medianoche hora VE, el reporte quedaba vacío sin ningún error visible, todas las noches, en esa ventana de ~4h | `ReporteAsistencias/ReporteRango.jsx` | ✅ Cerrado (2 ago) — encontrado porque `ReporteRango.integration.test.jsx` empezó a fallar de forma reproducible exactamente en esa ventana; corregido derivando "lunes" de la misma fecha VE que "hoy". `grep` confirmó que era la única ocurrencia restante del patrón en `src/` |
| **UX-28** 🔴 | Reportado por LS: *"probé a editar una clase y la siguiente se movía al lugar de la que edité, y cuando probé a agregarla nuevamente a la hora original, no apareció"* — `TurnoGrid.jsx` marcaba como "ocupados" los bloques cubiertos por el `rowSpan` de una clase y descartaba en silencio cualquier OTRA clase que empezara en uno de esos bloques, aunque siguiera intacta en `horarios` | `TurnoGrid.jsx`, `TurnoGrid.css` | ✅ Cerrado (2 ago) — reproducido sintéticamente antes de tocar código. Fix: las clases tapadas se fusionan en la celda que las tapa, marcada visualmente como conflicto (⚠️ + `--color-danger-bg`, mismas variables que `ConflictosView.css`) en vez de desaparecer. 3 tests nuevos (`TurnoGrid.test.jsx`). 224/224 tests, 0 errores de lint, build limpio |
| **UX-29** 🔴 | Reportado por LS tras `UX-28` (con screenshot): el bug persistía en un caso distinto — al mover una clase de día, la clase VECINA (con `hora` en formato compartido, ej. `"3:15-5:30PM"` sin AM/PM en el inicio — atajo común en carga manual) se veía "subir" a ocupar toda la columna desde la primera fila del turno, aunque su horario real no había cambiado | `utils/time.js`, `utils/turno.js`, `utils/conflictos.js` | ✅ Cerrado (3 ago) — causa raíz real: `timeToMin("3:15")` (sin sufijo) no matchea el regex y devuelve `0` en silencio; ese `0` se usaba tal cual en `findStartBlock`/`countBlocks`, posicionando la clase en el primer bloque del turno con un `span` estirado a toda la grilla. Nueva función `partesHoraNormalizadas()` (hereda el AM/PM del final si el inicio no trae el suyo) usada en las 5 funciones que antes duplicaban el mismo parseo por separado, incluyendo `parseRango` (`conflictos.js`) — que ya se protegía de este caso descartándolo (`if inicio===0 return null`), así que estos conflictos tampoco se detectaban en la pestaña Conflictos hasta este fix. Reproducido el caso exacto del screenshot de LS (mismos nombres/horas) antes y después del fix. 9 tests nuevos (`turno.test.js`, `conflictos.test.js`). 232/232 tests, 0 errores de lint, build limpio |
| **UX-30** 🔴 | Reportado por LS: PNF Agroalimentación usa un turno único "MIXTO" continuo 7:00am-4:00pm (sin el corte 12:00-1:00 de DIURNO/VESPERTINO), duración variable (45min–4h30) — la grilla de bloques fijos de 45 min por turno truncaba/ocultaba estas clases | `constants/index.js`, `utils/{turno,time,excelParser}.js`, `TurnoGrid.jsx`, `HorariosView.jsx`, `ModalEditarClase.jsx`, `ReporteRango.jsx`, `MateriasView.jsx`, `PlanillaImprimibleBase.jsx`, `ReporteAsistencias/index.jsx`, `AdminQRPanel.jsx` | ✅ Cerrado (4 ago, verificado contra el Excel real de Agroalimentación). 3 causas raíz iniciales: **(1)** `excelParser.js` priorizaba el código de hoja sobre el metadato TURNO explícito, pisando "MIXTO" con "DIURNO" por coincidencia de patrón — prioridad invertida. **(2)** `partesHoraNormalizadas()` (de `UX-29`) asumía mismo AM/PM en inicio/fin — falso en MIXTO, que cruza el mediodía — ahora valida duración positiva y prueba el AM/PM contrario si hace falta. **(3)** Grilla y modal asumían bloques fijos de 45 min por turno — nueva `buildBloquesDinamicos()` arma filas desde los horarios reales cargados. Turno `MIXTO` agregado a `TURNOS_CONFIG` (mismo mecanismo que `NOCTURNO`). Bug adicional en el propio fix: `bloqueValue()` desambiguaba por hora sin turno, MIXTO tarde guardaba "Vespertino" en silencio — corregido incluyendo turno en la clave. **3 follow-ups el mismo día** (payload de guardado sin `turno`; 5 lugares más con ternario binario DIURNO/VESPERTINO que ignoraban MIXTO en `ReporteRango.jsx`/`MateriasView.jsx`/`PlanillaImprimibleBase.jsx`/`ReporteAsistencias/index.jsx`/`AdminQRPanel.jsx`; cobertura de test pendiente cerrada). 33 tests nuevos en total. Verificación final: 264/264 tests, 0 errores de lint, build limpio |
| **UX-31** 🟡 | El trigger `autocompletar_sede_id` (`0063`) devuelve un mensaje técnico ("...tu perfil no tiene una sede fija asignada. Si tu rol ve todas las sedes, manda sede_id explícito.") y varios puntos del cliente lo concatenan crudo en el toast/mensaje de error al crear un docente/materia inline sin sede asignada | `ModalEditarClase.jsx`, `useAppData/nameEditing.js` | — | ✅ Cerrado (6 ago) — helper nuevo `utils/errorMessages.js` (`mensajeAmigable()`) intercepta puntualmente ESE mensaje y lo traduce a algo accionable para un usuario final; cualquier otro mensaje de error pasa sin cambios, sin inventar traducciones sobre casos no verificados. 3 tests nuevos. 280/280 tests, 0 errores de lint |
| **UX-32** 🟢 | `mensajeAmigable()` (fix de `UX-31`) solo traducía el mensaje del trigger de sede — cualquier otro error de Postgres (duplicados, FK) llegaba crudo al toast bajo carga real, sin ser accionable para un usuario no técnico bajo presión temporal (auditoría de estrés operacional, 6 ago) | `src/utils/errorMessages.js` | — | ✅ Cerrado (6 ago) — patrón de `UX-31` generalizado de un `if` único a una lista `TRADUCCIONES` (`match`/`mensaje`); agrega traducción para violación de constraint único (`duplicate key value violates unique constraint`) y de foreign key (`violates foreign key constraint`). Fallback intacto: ningún mensaje no cubierto se traduce a ciegas. 2 tests de regresión nuevos + test de fallback actualizado. 282/282 tests, 0 errores de lint, build limpio |
| **UX-33** 🟡 | Propuesta externa (LS le pidió a Grok una revisión). 2 gaps reales confirmados: **(A)** el token QR rota en cada marca (anti-captura-de-pantalla) — un docente primerizo que tardara en llenar el formulario perdía lo tecleado si expiraba antes de enviar; **(C)** `AdminQRPanel.jsx` no mostraba cuántos docentes se esperaban ese turno ni resumen al cerrar sesión. (B, offline-first, descartado — ya estaba completo) | `DocenteScan/{cedula,index,PasoValidacionCedula}.jsx`, `AdminQRPanel.jsx`, `utils/time.js` | `0072` | ✅ Cerrado (7 ago) — **(A)** borrador con debounce en clave separada (TTL 20 min), nunca se mezcla con la identidad confirmada. **(C)** RPC `contar_docentes_esperados` (sede-scoped, mismo patrón que `0067`) para el denominador "de N esperados"; resumen automático al cerrar sesión. Nuevo helper `diaSemanaVE()` (ancla UTC-4 explícita). Colisión de migración `0072`/ID `UX-31` detectada y renombrada antes de aplicar. 12 tests nuevos, 311/311 tests, 0 errores de lint, build limpio |
| **UX-34** 🟡 | Reportado por LS: en Asistencias, en móvil "no aparecen todos los pills". Causa raíz: `.asm-tabs` (grupo de pestañas del shell compartido por `AsistenciasModulo.jsx`/`AdminModulo.jsx`) es hijo flex sin `min-width: 0` — nunca se encogía por debajo del ancho de su contenido, empujando el resto de la topbar (badges, `UserMenu`) fuera del viewport sin overflow posible | `src/index.css` (`.asm-topbar`/`.asm-tabs`) | — | ✅ Cerrado (9 ago) — `.asm-tabs` con `overflow-x: auto`/`min-width: 0`, cada `.asm-tab` con `flex-shrink: 0`/`white-space: nowrap`. `@media (max-width: 640px)` reduce padding para necesitar menos scroll. 386/386 tests, 0 errores de lint, build limpio |
| **UX-35** 🟡 | Sin verificación automatizada de accesibilidad (WCAG) ni testing visual cross-browser — `UX-11` solo cubría Chromium, relevante por iOS/Safari | `playwright.config.js`, `tests/visual/a11y.spec.js`, `.github/workflows/ci.yml` | — | ✅ Cerrado (10 ago, graduado a bloqueante 12 ago) — proyecto Playwright `desktop-webkit-1280` + proyecto `a11y` (`@axe-core/playwright`, falla en violación WCAG `critical`/`serious`, `color-contrast` deshabilitado por falsos positivos), ambos sobre las pantallas de `UX-11` (login/selector/escaneo QR). **Ampliaciones sucesivas:** 11 ago, LS notó que faltaba mobile/tablet (el escenario real es un teléfono, no escritorio) → agregados `mobile-webkit-375`/`tablet-webkit-768`, matriz completa 3 breakpoints × 2 motores. 12 ago (`847daa4`, corrida `85855045178`): graduado a bloqueante, 9/9 WebKit + 3/3 a11y sin falsos positivos. 14 ago: ampliado a 2 pantallas admin (`Usuarios y Roles`, `Sedes y Programas`) vía `FAKE_PROFILE_ADMIN` en `mockSupabase.js`. Sigue sin cubrir el shell `.asm-tabs` (`UX-34`) — ver `UX-39` para `color-contrast` en las 2 pantallas admin |
| **UX-37** 🔴 | Auditoría UI/UX de élite (14 ago, simulación de personal institucional operando desde smartphone): `.s-btn`/`.s-select`/`.s-input` (`src/index.css`, usadas en 13 archivos, mayormente paneles Admin/Sistema) rondaban 27-30px de alto real (`padding: 7px 16px/12px` + `font-size: 13px`) — por debajo del mínimo táctil de 44-48px (WCAG 2.5.5 / Material). Un coordinador de sede gestionando roles/sedes desde el teléfono, en el contexto operativo ya documentado (cortes eléctricos de 5+ horas, señal móvil pésima — ver `OFF-10`), cae directo en esta clase | `src/index.css` | — | ✅ Cerrado (14 ago) — `min-height: 44px` agregado a las 3 clases, acotado a `@media (hover: none) and (pointer: coarse)` en vez de por ancho de viewport, para no inflar estos mismos controles en desktop/laptop con mouse (donde el problema no existe) y cubrir también tablets/laptops táctiles, no solo móviles angostos. 566/566 tests, 0 errores de lint, build limpio |
| **UX-38** 🟢 | Auditoría UI/UX de élite (14 ago): `.s-btn`/`.eb-btn`/`.bs-btn`/`.s-select`/`.s-input` reimplementaban el mismo concepto (padding de control interactivo) como literal en 5+ lugares sin variable compartida — cambiar el tamaño "estándar" implicaba grep manual en vez de tocar un solo punto. 5 de esas clases además hardcodeaban `border-radius: 8px` en vez de usar `--border-radius-md`, ya existente en el sistema de tokens pero no aplicado ahí | `src/index.css` | — | ✅ Cerrado (14 ago) — tokens nuevos `--field-pad`/`--btn-pad-sm`/`--btn-pad-md`/`--btn-pad-cancel`/`--btn-pad-lg` junto a la escala de espaciado existente, nombrados por valor real (mismo criterio que `--space-Npx`) sin fusionar tamaños que hoy son distintos a propósito — cero cambio visual, solo tokenización. Las 5 clases pasan también a usar `--border-radius-md` en vez del literal. 566/566 tests, 0 errores de lint, build limpio |
| **UX-39** 🟢 | Auditoría UI/UX de élite (14 ago), hallazgo colateral al ampliar `UX-35`: la regla `color-contrast` de axe-core estaba deshabilitada de forma global en las 3 pantallas del piloto por los falsos positivos reales de `login` (gradientes/overlays semi-transparentes) — pero esa excepción se heredaba a ciegas a cualquier pantalla nueva que se agregara al piloto, sin volver a verificar si el motivo original aplicaba | `tests/visual/a11y.spec.js` | — | ✅ Cerrado (14 ago) — en las 2 pantallas nuevas de `UX-35` (seguimiento) `color-contrast` corre **sin deshabilitar**: son tablas/formularios de fondo blanco sólido, sin gradientes ni overlays — el motivo original del falso positivo no aplica ahí. Las 3 pantallas originales (login/selector/escaneo QR) mantienen la excepción intacta, sin tocar. **Verificación (12 ago, corrida real `85855045178`):** las 2 pantallas nuevas con `color-contrast` activo pasaron limpias, sin violación `serious`/`critical` — confirma que eran tablas/formularios de fondo sólido sin el problema de overlays de `login`, tal como se esperaba. Mismo run que graduó `UX-35` a bloqueante |
| **UX-41** 🔴 | Auditoría UI/UX de élite (15 ago): `.mu-backdrop` (`ModalUsuario.jsx`) sin `overflow-y: auto` — a diferencia de `ModalRol.css` (mismo tipo de modal, ya corregido con `align-items: flex-start; overflow-y: auto`), el modal de crear/editar usuario centraba con `align-items: center` sin scroll de respaldo. En viewport corto (teclado virtual abierto en móvil), campos/botón "Guardar" quedaban inalcanzables. Mismo gap encontrado en `GestionSedes.css`/`HistorialView.css` (modal alta/edición de sede y `ModalTrimestre`), menor severidad por formularios más cortos pero mismo patrón de fondo | `src/components/usuarios/ModalUsuario.css`, `GestionSedes.css`, `HistorialView.css` | — | ✅ Cerrado (15 ago) — mismo patrón ya probado en `ModalRol.css` replicado en los 3 archivos: backdrop `align-items: flex-start` + `overflow-y: auto`, diálogo con `margin: 24px auto` en vez de centrado rígido. 571/571 tests, 0 errores de lint, build limpio |
| **UX-42** 🔴 | Auditoría UI/UX de élite (15 ago): `UX-37` (14 ago) corrigió targets táctiles <44px solo en `.s-btn/.s-select/.s-input` (`src/index.css`) — no cubrió botones de icono en CSS de componente: `.qrp-hist-borrar-btn` (26×26px, borrar sesión en `HistorialSesiones.jsx`, panel usado en campo por coordinadores — mismo contexto operativo de `OFF-10`) ni `.mr-color-swatch` (24×24px, selector de color de rol en `ModalRol.jsx`) | `src/components/asistencias/AdminQRPanel.css`, `src/components/usuarios/ModalRol.css` | — | ✅ Cerrado (15 ago) — mismo criterio exacto que `UX-37`: `min-width/min-height: 44px` acotado a `@media (hover: none) and (pointer: coarse)`, sin inflar los controles en desktop/mouse. `.mr-color-row` ya usaba `flex-wrap`, así que agrandar el swatch no rompe el layout. 571/571 tests, 0 errores de lint, build limpio |
| **UX-43** 🟡 | Auditoría UI/UX de élite (15 ago): `ModalUsuario.jsx` (`handleSave()`) validaba campo por campo con `return` en el primer error — un usuario con 3 campos inválidos a la vez necesitaba 3 intentos de "Guardar" para enterarse de todos. Único mensaje en banner genérico (`.mu-error`) al final del modal, sin `aria-invalid` ni asociación al campo específico | `src/components/usuarios/ModalUsuario.jsx`, `.css`, `src/index.css` | — | ✅ Cerrado (15 ago) — nuevo `validar()` recolecta todos los errores de una vez (`fieldErrors`, objeto por campo) en vez de cortar en el primero; cada input/select gana `aria-invalid`/`aria-describedby` y su propio `<p className="mu-field-error">` junto al campo. El foco salta automáticamente al primer campo inválido en el orden visual del formulario (no el orden de inserción del objeto). `error` (banner) queda reservado solo para fallos del guardado en sí (red/RPC/API), no para validación de formulario. Estilo `[aria-invalid="true"]` agregado de forma global a `.s-input`/`.s-select` en `index.css` — reutilizable por cualquier formulario futuro que adopte el mismo patrón, no solo este modal. 5 tests nuevos (`ModalUsuario.test.jsx`: múltiples errores simultáneos en un mismo intento, `aria-invalid` por campo, foco al primer campo inválido en orden visual, form válido sin fieldErrors residuales, corregir un campo no borra los demás pendientes). 571/571 tests, 0 errores de lint, build limpio |
| **UX-40** 🟡 | Reportado por LS (15 ago): trimestre `2-2026` cerrado aparecía como "abierto" en la tarjeta "Trimestre en curso" de `HistorialView.jsx`, contradiciendo la tabla de historial debajo. Causa raíz doble: **(1)** badge hardcodeado a `estado="activo"`, sin leer el estado real; **(2)** `lapsoActivo` es el lapso *seleccionado*, no necesariamente el que tiene `estado='activo'` en `trimestres` | `src/components/HistorialView.jsx` | — | ✅ Cerrado (15 ago) — badge corregido a `estado={trimestreActual?.estado}`. Bug colateral: el botón "Cerrar trimestre activo" podía re-cerrar el lapso mostrado en vez del que seguía activo, sin aviso — ahora solo se muestra si `lapsoActivo` coincide con el trimestre real activo. Verificado en Supabase real: sin filas corruptas. 11/11 + 7/7 tests sin cambios |
| **UX-44** 🟢 | Auditoría UI/UX de élite (14-15 ago), mismo hueco de targets táctiles que `UX-42` en otros componentes de icono: `.dv-edit-btn`/`--sm` (`DocentesView.css`) y análogos en `MateriasView.css` por debajo de 44px; `ModalRol.jsx` reproducía el mismo hallazgo de `UX-43` (validación campo-por-campo con corte en el primer error) | `src/components/DocentesView.css`, `src/components/MateriasView.css`, `src/components/usuarios/ModalRol.{jsx,css}` | ✅ Cerrado (15 ago) — mismo criterio `@media (hover: none) and (pointer: coarse)` que `UX-37`/`UX-42` para los botones de icono; `ModalRol.jsx` adopta el mismo patrón de `validar()`/`fieldErrors`/foco-al-primer-inválido que `UX-43` sentó en `ModalUsuario.jsx`. **Nota de proceso:** implementado y verificado, agregado retroactivamente a este índice hoy |
| **UX-45** 🟢 | Verificación responsive de seguimiento tras `UX-43`/`UX-44` (14 ago): colisión de nombre de clase entre `GlobalSearch.css` (`.gs-*`, "global search") y el prefijo ya usado en otros archivos por Gestión de Sedes — riesgo de que un cambio futuro en un componente pisara estilos del otro sin que ningún test lo detectara | `src/components/GlobalSearch.css` | ✅ Cerrado (14 ago) — renombrado de `gs-root` a `gsearch-root` en todo `GlobalSearch.css`, sin cambio visual. **Nota de proceso:** agregado retroactivamente a este índice hoy |
| **UX-46** 🟢 | Auditoría UI/UX de élite (15 ago), mismo hueco de targets táctiles en un tercer grupo de componentes: variantes de `.dv-edit-btn` no cubiertas por `UX-44`, botones de icono equivalentes en `MateriasView.css`, y `.gs-chip` (toggle de programa activo/inactivo en `GestionSedes.css`, `TabAsignacion`) por debajo de 44px | `src/components/DocentesView.css`, `src/components/MateriasView.css`, `src/components/GestionSedes.css` | ✅ Cerrado (15 ago) — mismo criterio `@media (hover: none) and (pointer: coarse)` que el resto de la serie `UX-37/42/44`. **Nota de proceso:** agregado retroactivamente a este índice hoy |
| **UX-47** 🟡 | Auditoría UI/UX de élite (15 ago, verificación independiente): `UploadPreviewModal.jsx` declara `role="dialog" aria-modal="true"` pero no tenía `useFocusTrap` (`UX-3`) ni cierre por `Escape` — Tab podía escapar el foco hacia el sidebar de fondo, y un usuario de teclado no tenía forma estándar de descartar la vista previa de carga. `ModalCambiarPassword.jsx` sí cerraba con Escape (patrón ya establecido) pero tampoco atrapaba Tab | `src/components/UploadPreviewModal.jsx`, `src/components/ModalCambiarPassword.jsx` | ✅ Cerrado (15 ago) — `useFocusTrap(dialogRef, open)` agregado a ambos (mismo hook que `ModalUsuario`/`ModalRol`/`ConfirmModal`/`ModalEditarClase`); `UploadPreviewModal.jsx` gana además cierre por `Escape` (montado solo mientras `open`, limpiado en el cleanup del efecto). Build limpio, 571/571 tests preexistentes sin regresión. **Sin tests de regresión nuevos**: ninguno de los dos componentes tenía suite propia antes de este fix — crear la infraestructura de mocks (`AppDataContext`/Supabase) para cubrirlos queda fuera de este fix puntual, mismo criterio de alcance acotado que `UX-11`/`UX-35` ya usaron |
| **UX-48** 🟡 | Auditoría UI/UX de élite (15 ago, verificación independiente): `AdminMenu.jsx`/`UserMenu.jsx` (dropdowns del sidebar/topbar) solo cerraban por clic-afuera (`mousedown`/overlay) — sin `Escape`, un usuario navegando solo con teclado no tenía forma de cerrarlos sin tabular hasta salir del documento, y ninguno devolvía el foco al botón que los abrió | `src/app/AdminMenu.jsx`, `src/app/UserMenu.jsx`, `src/app/HorariosSidebar.jsx` | ✅ Cerrado (15 ago) — listener de `Escape` agregado a ambos; `AdminMenu` recibe `triggerRef` nuevo desde `HorariosSidebar.jsx` (su botón vive en un componente padre distinto) y `UserMenu` usa un ref local (su botón es propio). Los dos devuelven el foco al trigger al cerrar por Escape, mismo criterio que `ConfirmModal` ya usa al cancelar. Build limpio, 571/571 tests preexistentes sin regresión. Mismo límite de alcance que `UX-47`: sin tests nuevos, cero cobertura previa en estos 2 componentes |
| **UX-49** 🟢 | Auditoría UI/UX de élite (15 ago), hallazgo colateral al ampliar `UX-35`/corregir el fixture de `UX-41`: 7 de los 14 valores de `--role-color` (`sb-admin`, `sb-coordinador`, `sb-verde`, `sb-administrativo`, `success`, `warning`, `cyan`) se usan como color de TEXTO directo en `.um-role`/`.shared-badge` sobre blanco, con contraste real de 1.92:1–3.77:1 — muy por debajo del 4.5:1 exigido, confirmado por axe-core en CI real, no solo estimado | `src/index.css` (bloque `.role-color--*`) | ✅ Cerrado (15 ago) — token nuevo `--role-text` agregado a las 14 variantes (igual a `--role-color` en las 7 que ya pasaban, versión más oscura de la misma familia verificada ≥4.5:1 en las 7 que fallaban), sin tocar `--role-color` (sigue siendo la identidad visual del swatch/checkbox, no texto). **Colisión de ID detectada y corregida hoy:** este fix estaba comentado en el código como `UX-42`, pisando el `UX-42` ya documentado (targets táctiles de íconos) — mismo patrón de colisión que el proyecto ya resolvió antes en `SEC-33`/`UX-33`; renumerado a `UX-49` en el comentario fuente, sin cambio funcional |
| **UX-50** 🟢 | Auditoría UI/UX de élite (14 ago): en `ConfiguracionReportes.jsx`, `.cr-layout` deja la vista previa del membrete al final del formulario en pantallas angostas (`flex-wrap`) — el usuario tenía que bajar hasta el fondo para ver el efecto de cada cambio y volver a subir para seguir editando el acordeón | `src/components/ConfiguracionReportes.jsx`, `.css` | ✅ Cerrado (14 ago) — barra sticky compacta nueva en móvil (`matchMedia("(max-width: 859px)")`, mismo membrete en un contenedor distinto, antes del formulario), reemplaza a `.cr-preview` (columna fija de desktop) cuando `esCompacta=true`. **Colisión de ID corregida hoy:** este fix estaba comentado como `UX-41`, distinto del `UX-41` ya documentado (scroll de backdrops de modal) — renumerado a `UX-50` en el código fuente, sin cambio funcional |
| **UX-51** 🟢 | Auditoría UI/UX de élite (15 ago), hallazgo colateral al ampliar `UX-35`: el fallback `.role-color--default` (rol con color fuera de las 14 conocidas, alcanzable en producción con datos legados anteriores a `UX-5`/`SEC-3`) usaba `var(--color-text-tertiary)` (#64748b) sobre `--role-bg` (#f3f4f6) — 4.32:1, por debajo del 4.5:1 exigido a texto normal, confirmado por axe-core en CI real | `src/index.css` (`.role-color--default`) | ✅ Cerrado (15 ago) — reemplazado por `var(--color-text-mid)` (#374151), 9.41:1 sobre el mismo fondo. **Colisión de ID corregida hoy:** este fix estaba comentado como `UX-41`, distinto del `UX-41` ya documentado y del `UX-50` de arriba — renumerado a `UX-51` en el código fuente, sin cambio funcional |
| **UX-52** 🟢 | Auditoría UI/UX de élite — verificación independiente (15 ago, clon fresco, equipo multidisciplinario simulado: frontend/UI-UX/design-system/a11y/QA). Cruzada contra las 51 entradas `UX-N` previas antes de evaluar: confirmado contra código real (no contra el índice) que no quedaban hallazgos bloqueantes ni de fricción media — guardas anti-doble-clic, `overflow-x` en las 12 tablas, estados vacíos en las 26 vistas con listas, focus-trap/`Escape` en modales y dropdowns, targets táctiles ≥44px y contraste de `--role-color` todos re-verificados en HEAD real. Único hallazgo nuevo: `ResumenView` (dashboard de tarjetas de métricas) sin límite de ancho — en monitores ultra-anchos (2560px+) las tarjetas se estiran con vacíos excesivos entre ellas | `src/components/ResumenView.css` (`.rv-root`) | ✅ Cerrado (15 ago) — `max-width: 1600px; margin-inline: auto;` agregado a `.rv-root`, acotado a esta vista y no al wrapper compartido `.hl-main`: ese wrapper también envuelve la grilla de horarios (`TurnoGrid`) y las tablas de reportes, que sí se benefician del ancho completo en pantallas grandes — un `max-width` ahí las habría constreñido sin necesidad. 571/571 tests, 0 errores de lint, build limpio |
| **UX-53** 🔴 | Reportado por LS: modal "Editar" de plantillas de reportes (`TabPlantillas.jsx`, Gestión de Reportes) se desbordaba. Causa raíz confirmada contra el código: `.tp-preview-wrap` (contenedor del preview en vivo) tenía `width: 300px` fijo sin `max-width: 100%` — en viewports angostos (~320-340px, celulares reales) el modal (`.gs-modal`, `max-width:780px`/`96vw`) solo tiene ~260-300px de ancho de contenido disponible tras su padding, así que ese `300px` rígido empujaba al modal (y al backdrop, sin `overflow-x`) a desbordar horizontalmente — justo el escenario que el propio comentario del archivo (fase 2, 12 ago) ya advertía que no debía pasar, pero el `max-width: 100%` que lo garantizaba nunca se aplicó a este contenedor en particular (sí estaba en `.tp-layout-canvas`, el otro contenedor de ancho variable del mismo modal) | `src/components/gestionReportes/TabPlantillas.css` (`.tp-preview-wrap`) | ✅ Cerrado (15 ago) — `max-width: 100%` agregado; el `overflow: hidden` ya existente recorta visualmente el iframe escalado (su caja sin transformar sigue siendo `dimensionPagina.w`, fijada por `ESCALA_PREVIEW`) sin que el contenedor fuerce el ancho del modal. En pantallas muy angostas el preview queda recortado en vez de desbordar — mismo criterio de degradación aceptado que el propio comentario del componente ya planteaba ("el preview no tiene mucho sentido útil por debajo de ~600px, pero no debe desbordar"). 571/571 tests, 0 errores de lint, build limpio |
| **UX-54** 🟡 | Reportado por LS: la vista previa del membrete en Reportes (`ConfiguracionReportes.jsx`) "no es muy útil, parece desactualizada". Confirmado contra el código: el propio archivo documenta desde su encabezado que esta vista previa NO reutiliza `reportePlantilla.js` — es una reimplementación manual en JSX que debe mantenerse sincronizada a mano con la plantilla real. Al comparar ambas se confirmó el drift: `logo_coordinacion_base64` (migración `0092`, 13 ago) tiene su propio campo de carga completo en el formulario (subir/cambiar/quitar) y se imprime realmente en el membrete (a la derecha del banner institucional, separador sutil — `reporte-print.css`), pero `renderPreviewCard()` nunca lo renderizaba — un admin podía subir ese logo sin ver jamás cómo queda en el preview | `src/components/ConfiguracionReportes.jsx` (`renderPreviewCard`), `.css` (`.cr-preview-logo-coordinacion`, nueva) | ✅ Cerrado (15 ago) — logo de coordinación agregado al preview, mismo criterio visual que la plantilla real: separador (`border-left`) en vez de tamaño para distinguirlo del logo institucional, sin alterar el resto del membrete si no está configurado (`&&` condicional, igual que el resto de los campos opcionales). 571/571 tests (10/10 de `ConfiguracionReportes.integration.test.jsx` sin cambios, no cubrían este campo antes), 0 errores de lint, build limpio |


*Nota de proceso (15 ago): `UX-44`, `UX-45` y `UX-46` estaban implementados y verificados en el código real (confirmado por `grep` contra 13+ archivos) pero nunca llegaron a tener fila en este índice — mismo patrón de drift que la nota de arriba ya advierte vigilar, en la dirección inversa (código adelantado a la documentación). `UX-41` sí estaba indexado, solo fuera de orden numérico (aparece antes de `UX-40` en este documento porque se agregó primero). Agregadas retroactivamente hoy: `UX-44`–`46`. Además, dos fixes reales estaban comentados en el código bajo IDs ya tomados por otro hallazgo — `UX-50` (vista previa sticky de `ConfiguracionReportes`, colisionaba con el `UX-41` de scroll de backdrops) y `UX-51` (contraste de `.role-color--default`, misma colisión) — renumerados hoy tanto en el código fuente como en este índice, sin cambio funcional. `UX-47`–`49` son hallazgos nuevos de esta misma auditoría, no reindexaciones.*

## 🎨 Identidad visual y sistema de diseño

Esquema `DESIGN-N` (antes `FE-N`). Fusionado desde `AUDITORIA_FRONTEND.md`
(documento eliminado tras la fusión).

| ID | Descripción | Archivo(s) clave | Estado |
|---|---|---|---|
| **DESIGN-1** | Iconografía funcional resuelta con emojis nativos del SO | `buildNavGroups.js`, `App.jsx`, etc. | ✅ Cerrado — cero emoji funcional (confirmado por grep de rango Unicode); sobrevive solo `EMOJIS_PRESET` (selector deliberado) |
| **DESIGN-2** | Tipografía sin identidad — solo `system-ui` | `src/index.css` | ✅ Cerrado — fuente Inter |
| **DESIGN-3** | Tokens de diseño incompletos — faltaba escala `--font-size-*` | `src/index.css`, objeto `S` | ✅ Cerrado (9 jul) — 21 variables definidas 1:1 de valores ya en uso, 569 sustituciones en 27 `.css`. `clamp()` responsivos y tamaños de una sola ocurrencia se dejan literales a propósito |
| **DESIGN-4** | Sin `:focus-visible` accesible consistente | `src/index.css` | ✅ Cerrado — 6 reglas |
| **DESIGN-5** | Adopción mixta de `var(--token)` en reglas `.hl-*` — algunos valores en px crudos | `src/index.css` (reglas `.hl-*`) | ✅ Cerrado (9 jul) — 17 reglas tokenizadas; 4 valores no múltiplo de 4 recibieron tokens exactos nuevos (`--space-6px` etc.) en vez de forzar la escala estándar |

---

## 🆕 Funcionalidad nueva

Esquema `ADMIN-N`. A diferencia del resto de este índice, documenta
funcionalidad nueva pedida directamente por LS (no hallazgos de auditoría) —
se incluye aquí porque el código ya usa el mismo formato de comentario.

| ID | Descripción | Archivo(s) clave | Migración | Estado |
|---|---|---|---|---|
| **ADMIN-1** | Borrado de registros de sesión/QR/reportes de asistencia, solo para admin | RPCs `admin_borrar_*` | `0054` | ✅ Cerrado (10 jul) — permiso dinámico en JSONB de roles, cada RPC revalida en servidor y registra en `audit_logs` |
| **ADMIN-2** | UI de borrado para `ADMIN-1`: selección múltiple, borrado por fila/rango | `TabSesiones.jsx`, `AdminQRPanel.jsx`, `ReporteRango.jsx` | `0054` | ✅ Cerrado (10 jul) — borrar sesión QR no borra asistencias ya registradas (`qr_session_id` → NULL) |
| **ADMIN-3** | Sacar "Usuarios y Roles"/"Registros"/"Historial" a un módulo propio, visible solo con permiso admin | `AdminModulo.jsx` (nuevo), `buildNavGroups.js`, `HorariosLayout.jsx` | — | ✅ Cerrado (10 jul) — nombre visible: "Sistema" (no "Administración", ya usado en el dropdown del sidebar). Historial pasa a exigir permiso admin (antes lo veía cualquiera con acceso a Horarios) |
| **ADMIN-4** | La jerarquía admin (`SEC-15`) ya bloqueaba en servidor crear/editar admins sin serlo, pero la UI no lo reflejaba | `usuarios/{index,PestanaUsuarios,ModalUsuario}.jsx` | — | ✅ Cerrado (10 jul) — `esActorAdmin` propagado; oculta "admin" del selector y bloquea edición sobre filas admin si el actor no lo es |
| **ADMIN-5** | Las 3 tarjetas del selector de módulo no caían en una fila en desktop; pedido general de optimización visual | `ModuleSelector.{jsx,css}` | — | ✅ Cerrado (12-13 jul, 3 pasadas) — Grid `auto-fit`/`minmax` en vez de `flexbox` de ancho fijo; rediseño compacto a layout horizontal tipo fila (180px→72px de alto); fix de `var(--color-text-primary)` que UX-13 (modo oscuro, en paralelo) habría roto en el fondo del splash |
| **ADMIN-6** | Pedido explícito de LS: personalizar el membrete de los 3 documentos imprimibles (logo, color, textos institucionales) — antes "UNERMB"/una "U" hardcodeados por separado en `exportPDF.js`, y `PlanillaImprimibleBase.jsx` sin membrete en absoluto | `configuracion_reportes` (migración `0056`), `reportePlantilla.js` (nuevo, compartido), `useReporteConfig.js` (nuevo), `ConfiguracionReportes.{jsx,css}` (nuevo, pestaña "Reportes" en Sistema), `exportPDF.js`, `PlanillaImprimibleBase.jsx`, `reporte-print.css` | `0056` | ✅ Cerrado (1 ago) — permiso granular nuevo `puedeConfigurarReportes`. Logo guardado como data URI en la fila (no Storage: el CSP `img-src 'self' data: blob:` ya lo permite sin abrir dominio nuevo; `CHECK` limita tipo a png/jpeg/webp — SVG excluido a propósito — y tamaño ~1.5MB). Color NO es hex libre: 6 clases CSS preset (`rp-color--*`), mismo criterio que `COLORES_PRESET`/`SEC-3` — el CSP `style-src 'self'` sin `unsafe-inline` bloquea tanto `style=""` en la ventana de impresión como en la app React normal (confirmado con los swatches del formulario, que también son clases, no `style={{background}}`). Unificar `exportPDF.js` y `PlanillaImprimibleBase.jsx` sobre la misma plantilla (`reportePlantilla.js`) corrigió de paso un bug latente nunca reportado: el `<style>` inline de `PlanillaImprimibleBase` casi con certeza estaba bloqueado en silencio por el mismo CSP que forzó externalizar `reporte-print.css` el 14 de julio. `ESC()` de la plantilla compartida ahora también escapa comillas (el logo va dentro de un atributo `src="..."`, no solo texto). 20 tests nuevos (`reportePlantilla.test.js`, `ConfiguracionReportes.integration.test.jsx`, casos nuevos en `AdminModulo.integration.test.jsx`) |
| **ADMIN-7** | Pedido de LS: "PWA completa + notificaciones push" para `/scan` y el panel de proyección. Primera pasada — solo instalabilidad/offline; push queda para una segunda pasada | `index.html`, `main.jsx`, `useInstallPrompt.js` (nuevo), `useModuloActivo.js`, `Shell.jsx`, `QRProyeccion.jsx`, 2 `manifest.webmanifest` nuevos | — | ✅ Cerrado (9 ago) — el app-shell offline ya funcionaba para cualquier ruta; faltaba la instalabilidad como apps propias. `/scan` y la proyección con manifest dedicado, intercambiado en runtime según la ruta. Meta `apple-mobile-web-app-*` agregados (sin ellos iOS no entra en modo standalone). De paso corregido: 2 `apple-touch-icon` en `index.html` donde el segundo pisaba al ícono real. Fix de deep-link en `useModuloActivo.js`: `?proyeccion=1` solo funcionaba con un único módulo asignado. Botón "Instalar" en `Shell.jsx`/`QRProyeccion.jsx` vía `useInstallPrompt.js` (con caso especial iOS/Safari, que no dispara `beforeinstallprompt`). 17 tests nuevos, 386/386 tests, build limpio |
| **ADMIN-8** (`PROG-4`) | Pedido de LS (12 ago): editar sedes activas y marcar qué programas están activos en cada sede, propagando el filtro a todo el sistema. Distinto de la serie `PROG-N` de arriba (esa es control de acceso; esto es catálogo — qué programas existen en cada sede) | `0090_prog4_catalogo_programas_y_sede_programa.sql`, `GestionSedes.jsx` (3 pestañas), `gestionSedes/{TabSedes,TabProgramas,TabAsignacion}.jsx`, `useProgramas.js`/`useProgramasActivosPorSede.js` | `0090` | ✅ Cerrado (12 ago) — tabla `programas` + relación `sedes_programas(sede_id, programa_id, activo)`, mismo patrón RLS que `sedes`/`0061`, sin DELETE. Propagado a `ModalUsuario.jsx` (checklist filtrado por sede), `AdminQRPanel.jsx` (selector por sede activa), `useNombresCache.js`. De paso corregido: `usuarios/index.jsx` recibía `programas` siempre vacío de `AdminModulo.jsx`. 547/547 tests, 0 errores de lint, build limpio. Migración `0090` confirmada aplicada en producción (verificado 16 ago, tablas `programas`/`sedes_programas` presentes) |

---

## 🗓️ Trimestre activo en Asistencias (`ASIST-N`)

Esquema `ASIST-N`, agregado el 12 de agosto: pedido directo de LS — el
módulo de Asistencias debe abrir por defecto en el trimestre actual, y
los trimestres cerrados deben quedar como historial de solo consulta,
igual que ya funcionaba en Horarios.

| ID | Descripción | Archivo(s) clave | Migración | Estado |
|---|---|---|---|---|
| **ASIST-1** | Hook compartido `useTrimestreActivo()`, fuente de verdad única sobre el trimestre activo/seleccionado. De paso corrige un bug real encontrado en la auditoría: el `lapso` inicial de Horarios (`App.jsx`) se calculaba con `getCurrentLapso()` — heurística por fecha de calendario — en vez de consultar `trimestres` de verdad. LS confirmó que los trimestres reales NO siguen el calendario fijo que asume esa heurística; con los datos de prueba actuales (trimestre "2-2026" cerrado el mismo 11 de agosto, activo real "3-2026" arranca 28-sep) la heurística sigue apuntando al cerrado — cualquiera que abriera Horarios caía en `modoConsulta = true` por defecto sin saberlo | `hooks/useTrimestreActivo.js` (nuevo), `App.jsx` | — | ✅ Cerrado (12 ago) — 535/535 tests, 0 errores/warnings de lint |
| **ASIST-2** | Aviso + bloqueo en Panel QR (`AdminQRPanel.jsx`) cuando la fecha de hoy cae fuera del rango de fechas del trimestre activo (mismo caso real que `ASIST-1`: un trimestre se cierra y el siguiente aún no arranca). `PlanillaQR.jsx` migrado al hook compartido (elimina el fetch duplicado a `trimestres` que tenía desde `ARCH-41`) | `AdminQRPanel.jsx`, `PlanillaQR.jsx`, `AsistenciasModulo.jsx` | — | ✅ Cerrado (12 ago) — 535/535 tests. Alcance reducido tras revisar el código real: el selector de fecha de `AdminQRPanel` está fijo a hoy (`min=max=hoy`), así que no hay forma de navegar ahí a sesiones QR de trimestres pasados — el riesgo de "borrar sesión de un histórico" que motivó el ticket no es alcanzable hoy. `ReporteAsistencias`/`EstadisticasAcademicas` ya son de solo consulta (confirmado en `ARCH-44`: no existe flujo de edición de `asistencias_diarias`) pero no tienen selector de trimestre — decisión consciente de no agregarlo hasta revisar su modelo de filtrado (ver `ASIST-4`) |
| **ASIST-4** | Selector de trimestre ("preset") en `ReporteAsistencias`, `ReporteRango` y `EstadisticasAcademicas`: salta Desde/Hasta (o Fecha, en la vista diaria) al rango del trimestre elegido — no reemplaza los campos, es un atajo editable después. Nuevo helper `rangoTrimestre()` en `utils/lapso.js` (recorta `fin` a hoy si el trimestre sigue en curso, evita devolver un rango invertido en el caso real detectado en `ASIST-1`: trimestre activo cuya `fecha_inicio` aún no llegó) | `ReporteAsistencias/index.jsx`, `ReporteRango.jsx`, `EstadisticasAcademicas/index.jsx`, `utils/lapso.js` (+ `rangoTrimestre()`, 4 tests) | — | ✅ Cerrado (12 ago) — 539/539 tests |
| **ASIST-5** | Hallazgo real durante `ASIST-4`, no solo UX: `admin_borrar_asistencias_rango()` (borrado real de `asistencias_diarias` por rango, solo admin) no validaba el trimestre en absoluto — un admin podía borrar asistencias de cualquier fecha, incluidas las de un trimestre cerrado hace meses. Guard agregado en servidor (rechaza si el rango no cae completo dentro del trimestre activo) + guard en cliente (botón "Borrar rango" deshabilitado con tooltip explicativo cuando el rango elegido no está vigente) | `ReporteRango.jsx` | `0089_asist4_5_guard_trimestre_borrar_asistencias_rango.sql` | ✅ Cerrado (12 ago) — aplicada en producción (Supabase project `fcrrtpujuncxruwxpckq`) y verificada |
| **ASIST-6** | Pedido de LS (12 ago), tras verificar en vivo el aviso de `ASIST-2`: no existía forma de corregir `fecha_inicio`/`fecha_fin` de un trimestre ya activo sin pasar por "Cerrar" (cambia `estado`) o "Nuevo trimestre" (bloqueado si el lapso ya está activo). Nuevo modo `editar` en `ModalTrimestre.jsx` + botón "Editar fechas" por trimestre en `HistorialLista.jsx`, protegido en servidor por la misma RLS ya existente (`puedeGestionarTrimestres`) — sin migración nueva. De paso se corrigió un bug real en el mismo modal: el modo "cerrar" no precargaba `fecha_inicio` (solo `fecha_fin`), así que cerrar sin retocarlo a mano lo dejaba en `NULL` por el `upsert` | `ModalTrimestre.jsx`, `HistorialView.jsx`, `HistorialLista.jsx` | — | ✅ Cerrado (12 ago) — 539/539 tests |
| **ASIST-7** | Hallazgo real, no de `ASIST-6`: LS probó "Editar fechas" y no vio ningún aviso, ni de éxito ni de error. Causa raíz: el módulo "Sistema" (`moduloActivo === "admin"` en `App.jsx`, donde vive Historial) se renderiza en un `return` separado de `HorariosLayout.jsx` — el único lugar de todo el repo donde se montaba `<Toast>`/`<ConfirmModal>`. `showToast()` sí actualizaba el estado (`appData.toast`) correctamente, pero nada lo pintaba — bug preexistente, no introducido por `ASIST-6`, que afectaba **todas** las acciones del módulo Sistema (cerrar/crear/editar trimestre, borrar usuario, etc.), no solo la nueva. Se encontró que el módulo de Asistencias tenía el mismo problema (recibía `showToast` pero nunca `toast`/`hideToast` para poder pintarlo) — corregido igual | `App.jsx`, `app/AsistenciasModulo.jsx` | — | ✅ Cerrado (12 ago) — 539/539 tests |

---



Esquema `SEDE-N`, agregado el 6 de agosto para cerrar `ARCH-37`: el sistema
de Sedes (independencia entre sedes de UNERMB — Cabimas, y las que se
agreguen) se construyó en 14 hallazgos/pasadas reales (commits `1f52fe9`
en adelante) que nunca se habían volcado a este índice. `SEDE-8` no existe
(hueco de numeración, sin código ni migración asociada — no se detectó
ninguna pérdida de contenido, a diferencia de los huecos de `ARCH-29`/`31`
o `ARCH-34`/`35`, que sí escondían trabajo no aplicado).

| ID | Descripción | Archivo(s) clave | Migración | Estado |
|---|---|---|---|---|
| **SEDE-1/2** | Catálogo de sedes + permiso `puedeVerTodasLasSedes` (en vez de fijo a una sola) | `sedes`, `user_profiles.sede_id`, `usuarios/{shared,ModalUsuario}.jsx` | `0061`, `0062` | ✅ Cerrado |
| **SEDE-3/4** | RLS de aislamiento por sede en `docentes`/`materias`/`horarios` (incl. particiones) y flujo anónimo `/scan` | `0063` | `0063`, `0064` | ✅ Cerrado |
| **SEDE-5** | Creación inline de docente/materia (`ModalEditarClase.jsx`, `+ Nuevo`) no mandaba `sede_id` — quedaban en la sede del trigger por defecto, no la sede activa elegida por un rol que ve todas las sedes | `ModalEditarClase.jsx` | — | ✅ Cerrado |
| **SEDE-6** | Badge de sede activa, primera versión (roles con `puedeVerTodasLasSedes`) | `ModuleSelector.jsx` | — | ✅ Cerrado |
| **SEDE-7** | `useSedes()` disparaba su fetch antes de que la sesión estuviera lista, carrera con el login | `useSedes.js` | — | ✅ Cerrado |
| **SEDE-9** | `conflictos_horario_detalle()` (`SECURITY DEFINER`) no filtraba por sede | `useConflictos.js`, migración de la época | — | ✅ Cerrado |
| **SEDE-10** | Badge de sede activa promovido a `UserMenu.jsx` (único elemento común a los 3 módulos, no solo `ModuleSelector`) | `UserMenu.jsx` | — | ✅ Cerrado |
| **SEDE-11** 🔴 | **Crítico:** `replace_nombre_en_clases()` (`0032`, junio 2026, nunca tocada desde entonces) era `SECURITY DEFINER` **sin ningún chequeo de permiso y sin filtro de sede** — cualquier autenticado podía reescribir `horarios.clase` de cualquier sede con solo conocer el nombre de la función. Encontrada en la pasada de auditoría de `SEC-28`/`29` (ver más abajo), ampliada al resto de funciones `SECURITY DEFINER` reales | `replace_nombre_en_clases` (`0032`), `nameEditing.js` | `0068` | ✅ Cerrado (6 ago) — exige `puedeEditarDocentes`/`puedeEditarMaterias` (nunca lo tuvo) y filtra el `UPDATE` por sede resuelta igual que `conflictos_horario` (`0067`) |
| **SEDE-12** | `admin_get_users`/`admin_toggle_user_activo`/`admin_delete_user`/`admin_reset_user_password` sin filtro de sede — hoy no explotable (solo admin/coordinador tienen `puedeGestionarUsuarios`, ambos con `puedeVerTodasLasSedes`), pero el RBAC es dinámico y un rol futuro "coordinador de sede" quedaría expuesto | 4 RPCs `admin_*` | `0068` | ✅ Cerrado (6 ago) — las 4 filtran ahora por `usuario_puede_ver_sede()` sobre la sede del usuario objetivo |
| **SEDE-13** | Mismo problema que `borrar_horarios`/`restaurar_backup` (`0065`) pero en dos RPCs que quedaron fuera de esa migración: `admin_borrar_qr_sesiones`/`admin_borrar_asistencias_rango` (`SECURITY DEFINER`) sin filtro de sede — un rol con `puedeBorrarSesiones`/`puedeBorrarReportes` en una sede podía borrar datos de todas | `admin_borrar_qr_sesiones`, `admin_borrar_asistencias_rango` (`0054`), `ReporteRango.jsx` | `0068` | ✅ Cerrado (6 ago) — mismo patrón de resolución de sede que `borrar_horarios` (`0065`); `admin_borrar_qr_sesiones` rechaza el lote completo si algún id no es visible para el actor, en vez de borrar parcial en silencio |
| **SEDE-14** | `renovar_qr_token()` solo validaba `puedeGestionarQR`, no que la sesión fuera de la sede del actor — defensa en profundidad, mismo principio que `SEC-8` | `renovar_qr_token` | `0068` | ✅ Cerrado (6 ago) — agrega `usuario_puede_ver_sede()` sobre la sede de la sesión |
| **SEDE-15** | `PlanillaQR.jsx` (pestaña "Planilla" de Asistencias QR) nunca leía `sedeActiva` — a diferencia del resto del módulo QR (`AdminQRPanel`/`ReporteRango`, que sí consumen `useSedeContext`), esta pestaña se autoabastece con su propio fetch a Supabase y quedó fuera de las pasadas `SEDE-3`–`14`. Sus 4 queries (`horarios` para la planilla, `horarios` para el selector de programas, `docentes_con_cedula()`/fallback a `docentes`, y `materias`) traían todas las sedes sin filtrar — con una sola sede cargada (Cabimas) nunca se notó, mismo patrón "nadie lo vio porque solo existía Cabimas" que `SEDE-9`/`0066`. Reportado por LS | `PlanillaQR.jsx` | — | ✅ Cerrado (6 ago) — consume `useSedeContext()` y agrega `.eq("sede_id", sedeActiva)`/`p_sede_id` a las 4 queries, mismo patrón que `useDataSync.js`/`useNombresCache.js`; 282/282 tests |
| **SEDE-16** | Revisión profunda a pedido de LS tras `SEDE-15`. RLS (`0063`/`0064`) ya aísla por sede a nivel de BD para roles con sede fija — el hueco real es solo para `puedeVerTodasLasSedes` (admin/coordinador general): RLS les deja pasar todas las sedes, el filtro de la sede activa del selector depende de que el componente lo mande explícito. 8 puntos sin ese filtro: `ModalEditarClase.jsx` (dropdown docentes/materias), `HistorialSesiones.jsx`/`AdminQRPanel.jsx` (historial QR del día), `ReporteAsistencias/index.jsx`, `VistaAusentes.jsx`, `AlertaSinVincular.jsx`/`exportCSV.js` (cruce de cédulas), `ReporteRango.jsx` (+ RPC sin `p_sede_id`), `useUpload.js` (falsos positivos de duplicados), `HistorialView.jsx` (estadísticas de trimestre) | los 10 archivos listados | `0069` | ✅ Cerrado (6 ago) — mismo patrón `.eq("sede_id", sedeActiva)`/`p_sede_id` en los 8 puntos; `0069` agrega `p_sede_id` a `reporte_asistencias_rango_agregado`. 282/282 tests, 0 errores de lint, build limpio |
| **SEDE-17** | A pedido de LS: el catálogo de `sedes` era intencionalmente de solo lectura desde la interfaz (altas/bajas quedaban "para una RPC futura si hace falta", nota original de `0061`) — se agrega el módulo de configuración. Nueva pestaña "Sedes" en el módulo Sistema (`GestionSedes.jsx`), gateada por un permiso granular propio `puedeGestionarSedes` (mismo criterio que `puedeConfigurarReportes`/`ADMIN-6` — no se reutiliza `puedeVerTodasLasSedes`: una cosa es VER todas las sedes en los reportes, otra ADMINISTRAR el catálogo en sí). Permite crear sedes (con `id`/slug generado del nombre e inmutable después — evita romper las FKs que ya lo referencian), editar nombre/orden, y activar/desactivar. Sin `DELETE` real a propósito: `sedes.id` tiene FKs entrantes desde `docentes`/`materias`/`horarios`/`qr_sessions`/`asistencias_diarias`/`user_profiles` — "dar de baja" es desactivar (`activa=false`, ya filtrado por `useSedes.js`), no borrar. Se agregó `refetchSedes()` a `useSedes.js`/`SedeContext` para que el selector de sede de toda la app se refresque tras un cambio sin recargar la página | `GestionSedes.jsx`, `GestionSedes.css`, `useSedes.js`, `SedeContext.jsx`, `App.jsx`, `AdminModulo.jsx`, `usuarios/shared.jsx` | `0070` | ✅ Cerrado (6 ago) — migración `0070` agrega el permiso `puedeGestionarSedes` (sin asignación automática a ningún rol, LS decide a quién dárselo) + políticas RLS de `INSERT`/`UPDATE` sobre `sedes` + constraints de formato de `id` y `orden >= 0`; 9 tests nuevos (`GestionSedes.test.jsx`), 293/293 tests, 0 errores de lint, build limpio |
| **SEDE-18** | Hallazgo incidental durante `SEDE-17`: `PestanaUsuarios.jsx` llamaba `useSedes()` **sin pasarle `userId`** — la guarda interna de `useSedes.js` (agregada en `SEDE-7` para no consultar antes de que exista sesión) se disparaba siempre, así que el selector de sede del modal de Nuevo/Editar usuario quedaba **permanentemente vacío** en producción, en silencio. El test de integración ya mockeaba `supabase.from("sedes")` pero nunca verificaba que se llegara a llamar, así que el mock estaba de adorno sin ejercitar el camino real | `PestanaUsuarios.jsx`, `usuarios/index.jsx` | — | ✅ Cerrado (6 ago) — `PestanaUsuarios` recibe `userId` como prop, `usuarios/index.jsx` le pasa `profile?.id`; test de regresión nuevo que confirma que `supabase.from("sedes")` sí se llama |

**Nota de reconciliación (6 ago):** `SEDE-11`/`13`/`14` los encontraron
en paralelo una auditoría externa (cerrada acá como `SEC-28`/`SEC-29`)
y esta pasada de LS, más completa (suma `SEDE-11`/`12`/`14` y mejor
validación en `SEDE-13`). La migración `0068` real es la de LS;
`SEC-28`/`29` quedan documentados como el hallazgo que originó el fix,
no como una migración aparte. `SEDE-15`, `SEDE-16`, `SEDE-17` y `SEDE-18`
son hallazgos posteriores y aparte, sin relación con esta reconciliación
— ver sus filas arriba.



Encontradas al construir `ESQUEMA_Y_MIGRACIONES.md`. Ya no se usan, pero los
archivos con estos comentarios siguen en el repo.

| Esquema | ID | Descripción | Archivo | Estado |
|---|---|---|---|---|
| `Fix #N` | **#2** | Políticas RLS `{public}` → `{authenticated}` en `user_profiles` | `0016_fix_rls_user_profiles.sql` | ✅ Cerrado |
| `Fix #N` | **#3** | FK duplicada bloqueaba el login (`PGRST201`) | `0017_drop_fk_duplicada_rol.sql` | ✅ Cerrado |
| `Fix #N` | **#4** | Recursión en `get_auth_role()` dentro de políticas RLS | `0016_fix_rls_user_profiles.sql` | ✅ Cerrado |
| `Fix #N` | **#8** | `borrar_horarios`/`restaurar_backup` sin verificación de permiso interno | `0018_fix_rpc_permisos_faltantes.sql` | ✅ Cerrado |
| `Fix #N` | **#10** | Sin trigger que impidiera borrar roles con `es_sistema = true` | `0019_trigger_protect_roles_sistema.sql` | ✅ Cerrado |
| `Fix #N` | **#16** | Sin índices en `horarios` para búsquedas frecuentes | `0020_indices_horarios.sql` | ✅ Cerrado |
| `Fix #N` | **#17** | RPCs de gestión de usuarios sin migración de respaldo | `0021_rpcs_gestion_usuarios.sql` | ✅ Cerrado |
| `Gap #N` | **#16** | `importarDatos()` no restauraba `asistencias` desde un backup | `0041_restaurar_backup_asistencias.sql` | ✅ Cerrado |

> **Colisión de numeración:** `Fix #16` y `Gap #16` son el mismo número en
> esquemas distintos, sin relación entre sí. Si se retoma cualquiera de
> estas nomenclaturas, evitar reusar números.

---

## 📝 Historial de auditorías (resumen por fecha)

Solo hitos — el "cómo" completo vive en las tablas de arriba.

- **jun 2026:** RLS inicial, QR/offline, migración a Tabler Icons + paleta slate.
- **4–9 jul:** auditoría QA externa + auditoría de arquitectura (87/100) — mayoría de `SEC-1`–`9`, `ARCH-1`–`9`, `UX-1`–`6`, `DESIGN-*`, `CI-*`. Reorganización del documento (9 jul, separar abiertos de historial), cierre de `ARCH-12`/`13`, `DESIGN-3`/`5`.
- **10–13 jul, 3 auditorías QA senior (Arq. 90-92, Seg. 93-96, UX 87-88):** `ADMIN-1`–`4`, `ARCH-14`–`22`, `SEC-15`/`18`–`22`, `UX-8`/`9`/`11`–`13`, la mayoría cerrados el mismo/día siguiente. Primera normalización de IDs (8-10 esquemas colisionantes → 8 prefijos únicos, 110 archivos).
- **14–16 jul:** reverso completo de `UX-13` (modo oscuro) a pedido de LS. Auditoría QA senior más sesión de implementación cierra `ARCH-23`–`25`, `SEC-24`, `UX-15`/`16`/`19`–`23`. Segunda normalización de IDs. 16 jul, primera corrida real de CodeQL: 2 alertas High (`SEC-23`/`25` — falso positivo parcial en `exportPDF.js`, XSS real corregido en `PlanillaImprimibleBase.jsx`); cierre de `UX-17`/`18`/`11`. Con esto, 0 hallazgos abiertos por primera vez. 181/181 tests.
- **1 ago:** rendimiento en reportes (`ARCH-26`–`28`, pedido explícito de LS), `npm audit` fallando en CI rastreado a dependencia de build no usada (`SEC-26`), personalización de reportes con logo/colores institucionales (`ADMIN-6`, corrige de paso un bug de CSS latente nunca reportado). 205/205 tests.
- **2 ago, auditoría de estrés operacional:** `ARCH-29`/`UX-24` reabiertos como `ARCH-31` (migración citada no existía en el repo), más `ARCH-30`, `SEC-27`, `ARCH-32`–`34`, `OFF-9`, `UX-25`–`28` (bug de horarios reportado por LS con `TurnoGrid.jsx`). 6 warnings de `exhaustive-deps` corregidos, `vite` `6→7`. 224/224 tests.
- **3 ago:** recurrencia del bug de `UX-28` en caso distinto (`UX-29`, con screenshot de LS). `ARCH-34` reabierto como `ARCH-35` (mismo patrón de fix-no-aplicado que `ARCH-29`/`31`). 232/232 tests.
- **4 ago:** turno "MIXTO" de PNF Agroalimentación incompatible con la grilla de bloques fijos (`UX-30`, 3 causas raíz + 3 follow-ups el mismo día). 264/264 tests.
- **6 ago, cinco pasadas independientes el mismo día:** (1) auditoría externa + reconciliación con 6 commits paralelos de LS — sistema de Sedes nunca auditado (`ARCH-37`), 2 `SECURITY DEFINER` sin filtro de sede (`SEC-28`/`29`), migración huérfana (`ARCH-36`); la migración real de LS (`0068`, `SEDE-11`–`14`) se usa sobre la propia, sin pisar nada. (2) verificación independiente contra HEAD real (`82aaa99`): reinstala, corre lint/tests/build desde cero (no confía en lo ya documentado), revisa línea por línea `0068` y `0059` (race condition reproducida contra Postgres real) — único hallazgo nuevo `UX-32`, cerrado el mismo día en paralelo también por LS de forma independiente (código idéntico, confirmado con `diff`). (3) `SEDE-15` (`PlanillaQR.jsx` no filtraba por sede activa — reportado por LS, quedó fuera de las pasadas `SEDE-3`–`14` por autoabastecerse con su propio fetch en vez de vivir en `AppDataContext`). (4) `SEDE-16`, revisión profunda a pedido de LS tras `SEDE-15`: confirma que RLS (`0063`/`0064`) ya aísla las tablas de sede a nivel de BD para roles con sede fija — el hueco real está acotado a roles con `puedeVerTodasLasSedes`, sin filtro de cliente para acotar a la sede activa elegida. 8 puntos corregidos (`ModalEditarClase.jsx`, `HistorialSesiones.jsx`/`AdminQRPanel.jsx`, `ReporteAsistencias/index.jsx`, `VistaAusentes.jsx`, `AlertaSinVincular.jsx`/`exportCSV.js`, `ReporteRango.jsx` + migración `0069` — agrega `p_sede_id` a `reporte_asistencias_rango_agregado`, `useUpload.js`, `HistorialView.jsx`). Verificación final: 282/282 tests, 0 errores de lint, build limpio.
- **6 ago, `SEDE-17`/`SEDE-18`, a pedido de LS tras confirmar el diseño de independencia por sede:** se agrega gestión de sedes desde la interfaz — hasta ahora el catálogo era intencionalmente de solo lectura (nota original de `0061`: "altas/bajas quedan para una RPC futura si hace falta"). Nueva pestaña "Sedes" en el módulo Sistema (`GestionSedes.jsx`), permiso granular propio `puedeGestionarSedes` (migración `0070`, sin asignación automática a ningún rol), alta/edición/activación sin `DELETE` real (las FKs entrantes desde docentes/materias/horarios/etc. hacen que "dar de baja" sea desactivar, no borrar). De paso se agrega `refetchSedes()` a `useSedes.js`/`SedeContext` para refrescar el selector de sede en vivo tras un cambio. Hallazgo incidental durante el trabajo (`SEDE-18`): `PestanaUsuarios.jsx` llamaba `useSedes()` sin `userId`, dejando el selector de sede del modal de usuarios permanentemente vacío en producción — corregido pasando `profile.id` desde `usuarios/index.jsx`. Verificación final: 293/293 tests (9 nuevos en `GestionSedes.test.jsx`), 0 errores de lint, build limpio.
- **7 ago, `SEC-30`/`31`/`32`, disparado por un error real de LS en producción:** al intentar cambiar la sede de un usuario, `admin_upsert_user_profile` fallaba con "function not found" — la migración `0062` (le agrega `p_sede_id` al RPC) nunca se había corrido contra la base real; LS la corrió a mano y confirmó la firma correcta con `pg_get_function_identity_arguments`. Se aprovechó para pedirle a LS una auditoría completa de `pg_policies` en toda la base (no solo lo tocado por `0061`–`0070`), que destapó tres problemas reales: (1) `SEC-30`, dos políticas SELECT huérfanas creadas a mano en algún momento — nunca en ninguna migración — sobre `qr_sessions`/`asistencias_diarias`, sin chequeo de sede, que por el OR de políticas permisivas anulaban el filtro correcto de `0064` y dejaban ver datos de todas las sedes; LS las borró en el momento, `0071` versiona el mismo `DROP` para que quede reproducible. (2) `SEC-31`, un hallazgo más serio y no relacionado con sedes: `session_logs` (la tabla de auditoría de sesiones) tenía desde `0031` una política `sl_insert` zombi (usa el viejo `get_auth_role()`) que nunca se borró y anulaba `sl_no_insert_directo` — cualquier usuario autenticado podía insertar filas de auditoría falsas sin pasar por el RPC. (3) `SEC-32`, dureza adicional: `qr_sessions` INSERT sin chequeo de sede en el `WITH CHECK` (bajo riesgo real porque la app solo crea sesiones vía RPC, pero cierra la brecha a nivel de RLS igual). LS aplicó la migración `0071` en Supabase y confirmó con `pg_policies` que las 3 quedaron cerradas: `session_logs` solo con `sl_no_insert_directo`+`ver_logs_sesion`, `qr_sessions` con `solo_hoy_insert_qr_sessions` (ya con chequeo de sede)+`lee_qr_sessions_por_permiso`, `asistencias_diarias` solo con `lee_asistencias_por_permiso`.
- **7 ago, `UX-33`, a partir de una revisión externa (LS le pidió a Grok propuestas de mejora):** de 3 propuestas (A/B/C), una (offline-first) resultó ya estar completa — descartada sin tocar código. Las otras dos sí eran gaps reales: **(A)** recuperación de borrador para el docente primerizo cuando el token QR rota a mitad del formulario (borrador en clave separada, TTL 20 min, nunca se mezcla con identidad confirmada). **(C)** contador "de N esperados" en el panel admin (RPC nueva `contar_docentes_esperados`, `0072`, sede-scoped) + resumen automático al cerrar sesión QR. Numeración de migración/ID de auditoría corregida antes de aplicar: `0069`/`UX-31` ya estaban tomados por `SEDE-16` y por trabajo previo del 6 ago respectivamente — renombrado a `0072`/`UX-33` sin pisar nada, confirmado sin conflicto real (`git merge` local limpio + verificación contra HEAD real con clon fresco). 311/311 tests, 0 errores de lint, build limpio.
- **7 ago, `SEC-33`, mismo día, auditoría completa de `EXECUTE` en toda función `SECURITY DEFINER`:** pedida por sospecha de que `0062`/`sl_insert` no fueran casos aislados. Confirmado: ~24 funciones administrativas sensibles (crear/borrar usuarios, resetear contraseñas, borrar reportes, restaurar backups, gestionar roles) aparecen ejecutables por `anon`/`PUBLIC` sin que ninguna migración se lo haya otorgado — mismo patrón que `SEC-8`/`SEC-9`, pero mucho más grande. Se verificó contra el código real cuáles RPCs se llaman legítimamente sin sesión (solo 4: `buscar_docente_scan`/`registrar_asistencia` del auto-escaneo QR, `verificar_bloqueo_login`/`log_login_fallido` del login) y se escribió la migración con el mismo patrón de `0052` (`DO $$ ... REGPROCEDURE`, resuelve la firma real desde `pg_proc`) para revocar `PUBLIC`/`anon` y confirmar `authenticated` en el resto. Al ir a versionarla se detectó que `UX-33` (trabajo paralelo, entrada de arriba, aplicado primero) ya había tomado el número `0072` — renumerada a `0073` sin pisar nada. LS la corrió y confirmó con la consulta completa de grants que las 32 funciones quedaron limpias.
- **7 ago, `SEC-34`, mismo día, al verificar el cierre de `SEC-33`:** se encontró la causa raíz de `SEC-8`/`SEC-9`/`SEC-33` — el rol `postgres` tenía una regla de privilegios por defecto (`pg_default_acl`) que otorgaba `EXECUTE` a `anon` automáticamente en toda función nueva del esquema `public`. Prueba en vivo: `contar_docentes_esperados` (`UX-33`, aplicada el mismo día) salió anon-ejecutable pese a que su propia migración hace `REVOKE ALL FROM PUBLIC` — nunca tuvo el privilegio vía `PUBLIC`, lo tenía por esta regla. `0074` corrige la regla en sí (`ALTER DEFAULT PRIVILEGES ... REVOKE EXECUTE ON FUNCTIONS FROM anon`) más el parche puntual en `contar_docentes_esperados`. LS corrió ambos comandos: el de `postgres` corrió y se confirmó con `pg_default_acl` que `anon` ya no aparece en la ACL por defecto de funciones de ese rol; el de `supabase_admin` falló con `permission denied to change default privileges` (rol interno de Supabase sin permiso para alterarlo) — sin impacto real, ningún flujo del proyecto crea funciones bajo ese rol.
- **8 ago, `OFF-10`/`OFF-11`, a pedido de LS tras reportar cortes eléctricos de 5+ horas diarios en el sitio de despliegue real:** diagnóstico inicial en conversación separada (sin tocar código) identificó que el offline existente (`OFF-1`/`OFF-7`) solo cubría continuidad dentro de una sesión QR ya activa, nunca el arranque en frío sin red, y que `TTL_MINUTES=5` acoplaba la cadencia de rotación del token con su margen de expiración real. Implementación: `OFF-11` desacopla `ROTATION_MINUTES` (5, fijo) de `EXPIRY_TTL_MINUTES` (360) en `useQRSession.js`. `OFF-10` combina pre-generación de sesiones offline con TTL extendido, cacheadas en IndexedDB (`qrOfflineCache.js`, nuevo) y activables sin RPC (`crearSesion()`), con un modo de respaldo manual sin token cuando no hay nada pre-generado (`manualAttendanceQueue.js`, nuevo, + RPC `registrar_asistencia_manual`, migración `0075`, solo `authenticated`, misma resolución de sede que `crear_qr_session` — se descartó explícitamente una variante con sesión 100% client-side por reabrir la categoría de bypass de RLS que `SEC-28`/`SEC-29`/`SEDE-11`–`14` vinieron a cerrar). `useSyncPendientes.js` gana cobertura de test por primera vez (no tenía ninguna) al extenderlo para sincronizar la cola manual. **Drift detectado antes de entregar:** la implementación se había hecho sobre un clon de 24h de antigüedad; 9 commits paralelos de LS (`SEC-30`–`34`, `UX-33`) habían tocado `AdminQRPanel.jsx`/`.css` y tomado el número de migración `0071` (usado por `SEC-30`/`31`/`32`) mientras tanto. Reconciliado a mano contra `origin/main` real: migración renumerada a `0075` (con `REVOKE` explícito de `anon` agregado, siguiendo el patrón que `SEC-33`/`34` establecieron ese mismo día), `AdminQRPanel.jsx`/`.css` re-integrados sobre la versión con `UX-33` ya aplicado — regiones no superpuestas en ambos archivos, sin pisar ninguna de las dos features. Verificación final: 346/346 tests (35 nuevos), build limpio. **Pendiente de LS:** aplicar la migración `0075` en Supabase real — hasta entonces los registros manuales quedan a salvo en IndexedDB sin poder sincronizar.
- **8 ago, `PERM-7`, encontrado de paso revisando el enforcement de `puedeHacerBackup` (propuesta D de Grok, ver `UX-33`):** reabre `PERM-4` — el fix documentado como cerrado nunca se aplicó al código real, `exportarDatos()` seguía apuntando a una tabla `asistencias` inexistente, y como ninguna de las 4 consultas del backup revisaba `.error`, todo backup exportado quedaba silenciosamente incompleto (`asistencias: []`) pero marcado como `asistencias_incluidas: true`. Corregido de raíz: tabla real (`asistencias_diarias`) + falla visible en vez de descarga incompleta silenciosa. `puedeVerTodo`/`puedeHacerBackup` en sí (la propuesta D original) siguen sin enforcement server-side — pendiente, este fix fue un hallazgo colateral encontrado en el camino hacia ese trabajo, no D en sí. 315/315 tests, 0 errores de lint, build limpio.
- **8 ago, `PERM-6`, cierre de la propuesta D (ver `UX-33`) para `puedeHacerBackup`:** `exportarDatos()` hacía 4 consultas directas del cliente sin ningún chequeo server-side de `puedeHacerBackup` — RLS por sede era el único freno real, y nunca comprobaba ese permiso. Reemplazadas por un RPC nuevo (`exportar_backup_completo`, `0076`, `SECURITY DEFINER`) que revisa el permiso antes de tocar cualquier tabla, con el filtro por sede repetido a mano (como bypasea RLS). A propósito sin fallback inseguro si el RPC no existe todavía. `puedeVerTodo` queda pendiente como serie propia `PROG-N` — es un problema del mismo tipo pero mucho más grande (toca todos los puntos de lectura de la app, no un archivo), no se resuelve de pasada. 6 tests nuevos. 352/352 tests, 0 errores de lint, build limpio.
- **9 ago, auditoría completa de BD contra un prompt avanzado de 11 secciones (esquema, drift, RLS, funciones `SECURITY DEFINER`, particiones, integridad referencial/índices/performance, multi-sede/programa, rate limiting/auditoría, timezone, Supabase-specific, integridad de datos), corrigiendo en simultáneo — pedido explícito de LS:** **Estado general: saludable**, 3 hallazgos menores encontrados y cerrados el mismo día (`SEC-35`/`36`, `ARCH-40` — ver tablas arriba, migraciones `0082`/`0083`). Confirmaciones fuertes obtenidas contra la BD real (no solo contra migraciones): `SEC-34` funciona de verdad — 3 funciones creadas después del 7 ago salen `anon=false` sin `REVOKE` propio, y `pg_default_acl` de funciones para `postgres` confirmado sin `anon`; `user_profiles` protegido contra escalada de privilegios, trigger `trg_proteger_columnas_sensibles_user_profiles` confirmado activo (`tgenabled='O'`); RLS idéntico entre `horarios` y sus 7 particiones; `pg_cron`/`SEC-21` confirmado activo (cierra un pendiente abierto desde el 6 ago); 0 registros huérfanos en las 3 pruebas de integridad referencial. Nota de proceso agregada a `SECURITY.md`: los privilegios por defecto de tablas/secuencias (a diferencia de funciones) siguen amplios para `anon`/`authenticated` — checklist nuevo para toda migración con `CREATE TABLE`. **Único pendiente real:** confirmar `SEC-7` (rate limiting nativo de Auth) en el dashboard — no verificable desde este entorno. **Reconciliación necesaria:** el commit `ADMIN-7` (mismo día, más abajo) se hizo sobre un clon anterior a `SEC-35`/`36`/`ARCH-40` y al pushear revirtió esas 3 filas de este índice (las migraciones `0082`/`0083` en sí no se tocaron) — mismo patrón de drift por trabajo en paralelo que ya documenta la nota de "Patrón recurrente a vigilar" arriba; reconciliado el mismo día sin perder ninguna entrada de ninguno de los dos lados.
- **9 ago:** `FLUJO_ASISTENCIAS_QR.md`/`MATRIZ_PERMISOS.md` resincronizados contra el código real — documentaban `UX-33` y el cierre de `PERM-6`/`PROG-N` como pendientes cuando ya estaban cerrados desde el 8 ago (confirmado clonando el repo y revisando migraciones/commits reales, no un resumen externo). `ADMIN-7`, pedido explícito de LS: primera pasada de "PWA completa" (mejora #4.3 del resumen de pendientes) — instalabilidad dedicada para `/scan` y la proyección QR, con fix de deep-link en `useModuloActivo.js` para que la PWA de proyección instalada funcione para usuarios con 2+ módulos. Push notifications quedan para una segunda pasada. 386/386 tests, build limpio.
- **9 ago (segunda pasada):** LS confirmó que las 6 migraciones de `PROG-N` (`0075`, `0077`–`0081`) ya se aplicaron en Supabase real. Verificado por conector directo (no por archivo SQL ni por `supabase migration list`, que las devuelve vacías): tabla `user_profiles_programas`, funciones `usuario_puede_ver_programa()`/`registrar_asistencia_manual()`/`exportar_backup_completo()`/`admin_get_user_profiles_programas()`/`admin_set_user_programas()`/`reporte_asistencias_rango_agregado()` confirmadas en `information_schema`/`pg_proc`, y las 4 políticas RLS de `horarios`/`asistencias_diarias` confirmadas en `pg_policies` referenciando el helper — el cierre real de `PROG-3 fase 3` ya está activo en producción, no solo en código. De paso, corregido un nombre de RPC mal transcrito en `RESUMEN_PENDIENTES.md` (`admin_set_user_profiles_programas` → `admin_set_user_programas`, el nombre real desde `0079`); el código fuente (migración y `ModalUsuario.jsx`) siempre usó el nombre correcto, el error era solo del resumen.
- **9 ago:** `ARCH-42`, cierra el pendiente de `ARCH-19` (33→24 warnings no bloqueantes de ESLint, detectados al verificar `ARCH-41`) — 5 de lógica real de hooks (`exhaustive-deps`) corregidos de verdad, no solo silenciados; 19 de organización de archivos (`only-export-components`) silenciados puntualmente con justificación, tras confirmar que separarlos exigía tocar ~40 puntos de import en todo el repo por un problema que solo afecta Fast Refresh en desarrollo. 24→0 warnings, 386/386 tests, build limpio.
- **9 ago (verificación operativa final):** cierre de los 3 pendientes de dashboard que quedaban desde la auditoría de BD. `pg_cron`/`SEC-21` y grants de las 8 funciones admin/sesión de `SEC-9`/`SEC-17` reverificados por conector directo — sin drift. Rate limiting nativo de Auth (`SEC-7`) confirmado activo y reforzado por captura de LS del dashboard real (sign-ups/sign-ins 4 req/5min por IP, resto en default) — el límite bajo de envío de emails (2/h) se descartó como riesgo real tras confirmar por `grep` que el código no usa ningún flujo de Auth por correo (`resetPasswordForEmail`/`inviteUserByEmail`/`signInWithOtp`/`magicLink`); `admin_reset_user_password` (`0014`) escribe la contraseña directo vía `SECURITY DEFINER`, sin pasar por el sistema de correo. Nuevo hallazgo del advisor de seguridad de Supabase: `auth_leaked_password_protection` deshabilitado (`SEC-37`) — toggle de dashboard, sin código ni migración, pendiente de decisión de LS.
- **9 ago (SEC-37, corrección de estado):** confirmado contra `get_organization` que la protección contra contraseñas filtradas requiere plan Pro — la organización (`bolysrglhpxjfydtuzkw`) está en plan `free`. `SEC-37` pasa de "pendiente de decisión" a "bloqueado por plan"; no es accionable hasta un eventual upgrade.
- **10 ago, auditoría de estrés operacional externa (simulación de concurrencia masiva, entradas maliciosas, fallos de infraestructura/conectividad y fatiga de usuario contra HEAD real, 403/403 tests, 0 lint, build limpio):** 4 hallazgos, ninguno bloqueante, cerrados el mismo día. `ARCH-43` corrige que el job de CI para `qr:load-test` (documentado por `ARCH-39` el 9 ago como "pendiente de confirmar") nunca había llegado realmente a `ci.yml` — mismo patrón de fondo que `ARCH-29`→`31`/`ARCH-34`→`35`; se resolvió con un workflow separado (`qr-load-test.yml`, cron semanal + manual), no dentro de `ci.yml`. `SEC-38` cierra que `mensajeAmigable()` (`UX-31`/`32`) era una whitelist *pass-through* — cualquier error de Postgres no cubierto se mostraba crudo al usuario, filtrando estructura interna de la BD; se agregó fallback genérico + 2 reglas nuevas + se conectaron 6 archivos que bypasseaban el filtro por completo. `OFF-12` agrega cola offline para cargas de horarios por Excel interrumpidas por corte de red (a diferencia de `OFF-10`, sin auto-sync — reintento manual explícito, porque el catálogo de docentes/materias pudo cambiar mientras tanto). `UX-35` agrega un piloto de accesibilidad WCAG (axe-core) y testing visual cross-browser (WebKit) sobre las 3 pantallas que `UX-11` ya cubre — ambos arrancan `continue-on-error: true` en CI por no haberse podido verificar contra navegadores reales en este entorno (sin salida de red hacia servidores de descarga de Playwright). **Colisión de ID detectada antes de aplicar:** el hallazgo de accesibilidad se iba a numerar `UX-34`, ya tomado (bug de pills responsive, cerrado el 9 ago) — renumerado a `UX-35` sin pisar nada, mismo criterio que `UX-33`/`SEC-33` ya aplicaron antes.
- **10 ago, drift paralelo detectado y reconciliado en 2 rondas durante la implementación de arriba:** primera ronda, 5 commits de LS (`SEC-35`/`36` ya reflejados arriba, más 4 migraciones de corrección `ON CONFLICT`/`sede_id` — numeradas originalmente `0082`/`0082b`/`0083`/`0084`, colisionando con los prefijos que ya usaban `SEC-35`/`36`, `ARCH-40` y la migración de estadísticas académicas — más `audit-on-conflict.yml`). Segunda ronda, 7 commits más: cobertura de tests nueva sin relación con esta auditoría (`api/admin-users.test.js` — el endpoint más sensible del sistema, sin ningún test hasta ahora —, `ModalRol.test.jsx`, `ModalUsuario.test.jsx`, `password.test.js`, `backupActions.restaurarBackup.test.js`) más el mismo fix de `ON CONFLICT` aplicado también al fallback client-side de `restaurarBackup()` (`backupActions.js`). `git merge` local sin conflictos de contenido en ambas rondas. **Revisando el contenido de las 4 migraciones de corrección:** cada una documenta en su propio comentario que ya se aplicó en producción el mismo 10 ago — sin impacto adicional más allá de la ventana real entre que `SEC-35` se aplicó (9 ago) y estas 4 se aplicaron (10 ago), durante la cual `registrar_asistencia()`/`registrar_asistencia_manual()`/`restaurar_backup()` habrían fallado con "no unique or exclusion constraint matching ON CONFLICT" ante cualquier marcaje real — **pendiente de LS confirmar el alcance real revisando logs de Postgres/API de ese rango**, no verificable desde este entorno (tool de Supabase sin aprobar). **Colisión de numeración resuelta:** las 4 migraciones renumeradas `0082`→`0085`, `0082b`→`0086`, `0083`→`0087`, `0084`→`0088` (contenido SQL sin cambios, ya aplicado en producción bajo los nombres viejos — renombrar el archivo del repo no reaplica nada). Verificación final: 472/472 tests, 0 errores de lint, build limpio.
- **11 ago:** LS pidió auditoría integral pre-producción (arquitectura/resiliencia, seguridad ofensiva, UX) con simulación de estrés (concurrencia, entradas maliciosas, fallos de infra, fatiga de usuario) contra HEAD real — 528/528 tests, 0 lint, build limpio. Veredicto: apto para producción (único hallazgo abierto en ese momento: `SEC-37`, bloqueado por plan free de Supabase — `SEC-39` se sumó en paralelo el mismo día, ver arriba). Dos hipótesis propuestas ese mismo día se descartaron al verificar contra código real en vez de asumir: **`CI-5`** (WebKit/a11y no bloqueantes en CI) resultó ya resuelto en paralelo por LS (`UX-35` ampliado a mobile/tablet-webkit el mismo día — ver fila arriba); **`ARCH-44`** (bloqueo optimista en asistencia manual) no aplicaba una vez revisado el código: `registrar_asistencia_manual` es `INSERT ... ON CONFLICT DO NOTHING`, no un `UPDATE` — la concurrencia ya es atómica a nivel de constraint único, y no existe ningún flujo de edición de `asistencias_diarias` en todo el repo. **`UX-36`** sí era un gap real: `esErrorDeRed()` (`OFF-12`) no reconocía sesión expirada, y la rama de `insertError` en `useUpload.js` nunca revisaba si el error era recuperable en absoluto — un JWT vencido a mitad de una carga grande de Excel perdía el archivo y forzaba rehacer todo el flujo. Cerrado con el mismo mecanismo de cola de `OFF-12`. 530/530 tests, 0 lint, build limpio.
- **14 ago, auditoría UI/UX de élite (equipo multidisciplinario simulado: arquitectura frontend, UI/UX, design system, accesibilidad WCAG, QA de experiencia — 4 ejes puntuados 0-100, clon fresco verificado contra código real, no contra este mismo índice):** puntuación 86/85/80/82 (consistencia visual / arquitectura de información / responsive / feedback-a11y). La mayoría de hallazgos "de primera pasada" ya estaban cerrados por las 36 entradas previas de `UX-N` — esta pasada solo encontró lo que quedaba genuinamente abierto, sin repetir nada ya resuelto. 4 hallazgos, todos cerrados el mismo día: `UX-37` (`.s-btn`/`.s-select`/`.s-input` bajo el mínimo táctil de 44px en paneles Admin, acotado a `pointer: coarse`), `UX-38` (padding/radio de 5 clases de botón tokenizado, sin cambio visual), `UX-35` seguimiento (piloto de a11y ampliado de 3 a 5 pantallas, cubriendo por primera vez el módulo Sistema/Admin) y `UX-39` (`color-contrast` habilitado en las 2 pantallas nuevas, en vez de heredar a ciegas la excepción de `login`). **Drift detectado y reconciliado durante la implementación:** LS aplicó los 3 fixes de código directo en `main` (`aeec84e`/`16bc470`/`1eea8d5`) mientras se redactaba este mismo índice sobre un clon de sesión — verificado con `git pull` antes de escribir esta entrada, contenido idéntico byte a byte al ya committeado, sin necesidad de reconciliar nada. 566/566 tests, 0 errores de lint, build limpio. **Pendiente de LS:** revisar la primera corrida real de los 2 tests de a11y nuevos en Actions (mismo bloqueo de sandbox sin red a servidores de Playwright que ya documentan `UX-11`/`UX-35`).

- **15 ago, auditoría UI/UX de élite (verificación independiente, clon fresco):** 2 hallazgos nuevos, `UX-47` (trampa de foco ausente en `UploadPreviewModal`/`ModalCambiarPassword`) y `UX-48` (dropdowns `AdminMenu`/`UserMenu` sin cierre por `Escape` ni retorno de foco), cerrados el mismo día. Al documentarlos se detectaron y corrigieron 3 colisiones de ID reales (`UX-42` y `UX-41` cada uno reusado para 2-3 fixes de contenido distinto sin relación entre sí) — renumerados a `UX-49`/`50`/`51`, mismo patrón que `UX-33`/`SEC-33` ya resolvieron antes. Se agregaron también al índice `UX-44`/`45`/`46`, implementados en código desde el 14-15 ago pero nunca indexados. **Incidente y corrección:** un primer intento de subir este lote (`fe733d9`) pegó por error contenido viejo de `GestionSedes.css`/`HistorialView.css`/`ModalRol.css`/`index.css`, borrando sin querer los fixes ya cerrados `UX-44`/`UX-46` y revirtiendo 9 colores a hex hardcodeado — revertido (`c499062`) y resubido correctamente (`89e90f9`), confirmado idéntico byte a byte contra un clon independiente del remoto, 571/571 tests. **Reconciliación de pendientes previos:** al revisar qué quedaba abierto se confirmó que 2 de los 3 "pendientes de LS" que este mismo índice arrastraba estaban ya resueltos antes de esta sesión, solo sin reflejar acá — `UX-35` (baselines WebKit móvil/tablet, generadas el 11 ago en `8371a62`) y la graduación a bloqueante del CI de WebKit/a11y (`847daa4`, 12 ago, corrida real `85855045178`, 9/9 + 3/3 sin falsos positivos). Notas de "pendiente" desactualizadas en `UX-35`/`UX-39` corregidas. Único pendiente real que queda en todo el índice: `SEC-37` (bloqueado por plan `free` de Supabase, no depende de código).
- **15 ago, `UX-52`, pasada adicional de verificación independiente (equipo multidisciplinario simulado: frontend/UI-UX/design-system/a11y/QA, clon fresco):** re-verificó contra código real (no contra este índice) las 51 entradas `UX-N` previas — guardas anti-doble-clic en los ~12 flujos de guardado, `overflow-x` en las 12 tablas del sistema, estados vacíos en las 26 vistas con listas condicionales, focus-trap/`Escape` en modales y dropdowns, targets táctiles ≥44px y contraste de `--role-color` — todo confirmado sin regresión. Único hallazgo nuevo: `.rv-root` (`ResumenView`, dashboard de tarjetas de métricas) sin `max-width`, se estiraba sin límite en monitores ultra-anchos (2560px+). Corregido con `max-width: 1600px; margin-inline: auto;`, acotado a esa vista — se descartó aplicarlo al wrapper compartido `.hl-main` porque también envuelve `TurnoGrid` y las tablas de reportes, que sí necesitan el ancho completo. 571/571 tests, 0 errores de lint, build limpio.
- **15 ago, `UX-53`, reportado por LS:** modal "Editar" de plantillas de reportes se desbordaba. Causa raíz: `.tp-preview-wrap` con `width: 300px` fijo sin `max-width: 100%` — en celulares angostos (~320-340px) el modal solo tiene ~260-300px de ancho de contenido disponible, así que el contenedor del preview forzaba el desborde. Corregido agregando `max-width: 100%`; el `overflow: hidden` ya existente recorta el preview en vez de desbordar en pantallas muy angostas. 571/571 tests, 0 errores de lint, build limpio.
- **15 ago, `UX-54`, reportado por LS:** vista previa del membrete en Reportes percibida como desactualizada. Confirmado: `logo_coordinacion_base64` (migración `0092`) tiene campo de carga completo en el formulario y se imprime en el membrete real, pero el preview manual en JSX nunca lo mostraba (drift entre la reimplementación y `reportePlantilla.js`, documentado desde el encabezado del propio archivo como riesgo conocido). Agregado al preview con el mismo criterio visual de la plantilla real (separador, no tamaño). 571/571 tests, 0 errores de lint, build limpio.
- **16 ago, reconciliación de pendientes contra el proyecto real (LS preguntó "algo pendiente?" y corrigió 2 supuestos de la respuesta anterior — verificado en vivo vía conector Supabase directo, no por reporte de LS ni por lectura del propio índice):** `OFF-10` confirmado cerrado — migración `0075` sí está aplicada en producción (`registrar_asistencia_manual()` existe en `pg_proc`, 7 parámetros; `supabase_migrations.schema_migrations` la registra junto a su fix de `ON CONFLICT`/`sede_id`), la nota de "pendiente" que este índice arrastraba estaba desactualizada. `SEC-39 (3)` (firma duplicada de `restaurar_backup()`) confirmado cerrado — sobrevive una sola firma, coincide exacto con la que llama `backupActions.js`. `SEC-37` reclasificado de "bloqueado por plan" a "no aplicable" — LS confirmó que no tiene planes de contratar Pro en el corto plazo, mismo criterio que `UX-13`. **Hallazgo no buscado, encontrado al verificar lo anterior:** `SEC-40`/`SEC-41` — RLS de `horarios` bloqueando escritura en trimestres cerrados a nivel de BD, con su propio fix de columna ambigua y una excepción vía GUC transaccional para `unificar_docente`/`_materia` — ya aplicados en producción (migraciones `0098`/`0099`, mismo día 15 ago) pero nunca habían entrado a este índice. Documentados retroactivamente a partir del SQL real de la migración (no de memoria ni de reporte de LS) — mismo patrón de drift documental que este índice ya viene señalando (`UX-35`/`39`, `ARCH-29`→`31`, `ARCH-34`→`35`, `ARCH-43`). No verificado en esta reconciliación: quién aplicó `0098`/`0099`/`0075`/`0086` ni cuándo exactamente (los timestamps de `schema_migrations` solo dan la hora de aplicación en el proyecto, no el autor), ni si hay cola manual de `OFF-10` real acumulada esperando sincronizar desde antes de que `0075` se aplicara.
- **16 ago, `ARCH-39`, LS subió el log real de la primera corrida en Actions (`qr-load-test.yml`, run `86633665685`, job "Concurrencia QR (registrar_asistencia)"):** el job corrió en verde en `ubuntu-24.04`, pero el log del paso "Correr load test de concurrencia QR" muestra exactamente el mensaje de salto documentado en la fila — `SUPABASE_URL`/`SUPABASE_ANON_KEY` presentes en el entorno, `SUPABASE_SERVICE_ROLE_KEY` vacío. LS agregó 1 de los 2 secrets que quedaban pendientes; el test de concurrencia real (`LOAD_TEST_CONCURRENCY` docentes simultáneos + la reproducción de la condición de carrera de `ARCH-33`) todavía no se ejecutó contra Supabase ni una sola vez. `ARCH-39` permanece 🟡 abierto — no se cierra con este log, que confirma justamente que falta el secret.
- **16 ago, `ARCH-39`, cerrado — LS agregó el secret faltante (`SUPABASE_SERVICE_ROLE_KEY`) y subió el log de la corrida siguiente (run `86635963366`, mismo workflow):** esta vez el paso corrió el script completo, con los 3 secrets presentes. Oleada throughput: 40/40 exitosos, 0 rechazos, 0 errores inesperados, p50=507ms. Oleada rate-limit-concurrente: 1 exitoso, 5 `RATE_LIMIT` (esperado), 9 `DEVICE_DUPLICADO`, 0 errores inesperados — verificado contra el código real de `registrar_asistencia()` (guard (e), migración `0085`) que `DEVICE_DUPLICADO` es un chequeo antifraude distinto y anterior al rate limit (mismo dispositivo ya usado por otra cédula en la sesión), esperado dado que el fixture de esta oleada usa 15 cédulas distintas sobre un solo `device_fingerprint` — no es una falla del rate limit ni del test. Limpieza confirmada en el log. Al revisar la fila para cerrarla se corrigió además una imprecisión propia: una versión anterior de este índice decía que el job usaba `continue-on-error: true` pendiente de "graduar a bloqueante" — verificado contra `.github/workflows/qr-load-test.yml` real, ese flag nunca estuvo en este workflow separado (`ARCH-43`, 10 ago); ya falla solo si el script detecta una regresión real, sin nada que graduar.

## 🔁 Tabla de equivalencias (IDs antiguos → nuevos)

Reorganización del 13-14 de julio de 2026. Si un commit viejo, un PR
cerrado, o una conversación pasada menciona un ID que no aparece en ninguna
tabla de arriba, buscarlo acá. `ADMIN-N` no está porque nunca colisionó.

**Nota:** las 12 migraciones SQL ya aplicadas a producción conservan a
propósito sus IDs *originales* en los comentarios (historial real de lo que
corrió contra la BD, no se tocaron en la reorganización). Si un comentario
dice `Fix S1`, es el mismo hallazgo que esta tabla mapea a `SEC-1`.

<details>
<summary><strong>SEC-N — Seguridad y RLS</strong></summary>

| Antiguo | Nuevo | | Antiguo | Nuevo |
|---|---|---|---|---|
| `S1` | `SEC-1` | | `V-4` | `SEC-12` |
| `S2` | `SEC-2` | | `D-3` | `SEC-13` |
| `S3` | `SEC-3` | | `D-6` | `SEC-14` |
| `SEC-2` | `SEC-4` | | `SEC-10` | `SEC-15` |
| `SEC-3` | `SEC-5` | | `SEC-11` | `SEC-16` |
| `SEC-5` | `SEC-6` | | `SEC-9` | `SEC-17` |
| `SEC-6` | `SEC-7` | | `D-7` | `SEC-18` |
| `SEC-7` | `SEC-8` | | `SEC-13` | `SEC-19` |
| `SEC-8` | `SEC-9` | | `SEC-14` | `SEC-20` |
| `V-1` | `SEC-10` | | `SEC-12` | `SEC-21` |
| `V-2` | `SEC-11` | | `SEC-15` | `SEC-22` |

</details>

<details>
<summary><strong>PERM-N — Filtrado de datos por permiso/programa</strong></summary>

| Antiguo | Nuevo |
|---|---|
| `V-3` | `PERM-1` |
| `D-1` | `PERM-2` |
| `D-2` | `PERM-3` |
| `D-4` | `PERM-4` |

</details>

<details>
<summary><strong>OFF-N — Offline y estado de red</strong></summary>

| Antiguo | Nuevo |
|---|---|
| `O-1` | `OFF-1` |
| `O-2` | `OFF-2` |
| `O-3` | `OFF-3` |
| `O-4` | `OFF-4` |
| `O-5` | `OFF-5` |
| `O-8` | `OFF-6` |
| `P-2` | `OFF-7` |
| `P-3` | `OFF-8` |

</details>

<details>
<summary><strong>ARCH-N — Arquitectura, testing y concurrencia</strong></summary>

| Antiguo | Nuevo | | Antiguo | Nuevo |
|---|---|---|---|---|
| `A1` | `ARCH-1` | | `ARCH-9` | `ARCH-12` |
| `A-2` | `ARCH-2` | | `ARCH-10` | `ARCH-13` |
| `A-3` | `ARCH-3` | | `ARCH-11` | `ARCH-14` |
| `A-4` | `ARCH-4` | | `ARCH-12` | `ARCH-15` |
| `A-5` | `ARCH-5` | | `ARCH-13` | `ARCH-16` |
| `A2` | `ARCH-6` | | `ARCH-14` | `ARCH-17` |
| `ARCH-4` | `ARCH-7` | | `ARCH-15` | `ARCH-18` |
| `ARCH-5` | `ARCH-8` | | `ARCH-16` | `ARCH-19` |
| `ARCH-6` | `ARCH-9` | | `ARCH-17` | `ARCH-20` |
| `ARCH-7` | `ARCH-10` | | `ARCH-18` | `ARCH-21` |
| `ARCH-8` | `ARCH-11` | | `ARCH-19` | `ARCH-22` |

</details>

<details>
<summary><strong>UX-N — UI y estilos</strong></summary>

| Antiguo | Nuevo | | Antiguo | Nuevo |
|---|---|---|---|---|
| `U-1` | `UX-1` | | `U-8` | `UX-9` |
| `U-2` | `UX-2` | | `U-9` | `UX-10` |
| `U-3` | `UX-3` | | `U-10` | `UX-11` |
| `U-4` | `UX-4` | | `U-11` | `UX-12` |
| `A3` | `UX-5` | | `U-12` | `UX-13` |
| `U-5` | `UX-6` | | `U-13` | `UX-14` |
| `U-6` | `UX-7` | | `U-14` | `UX-15` |
| `U-7` | `UX-8` | | `U-15` | `UX-16` |

</details>

<details>
<summary><strong>DESIGN-N — Identidad visual y sistema de diseño</strong></summary>

| Antiguo | Nuevo |
|---|---|
| `FE-1` | `DESIGN-1` |
| `FE-2` | `DESIGN-2` |
| `FE-3` | `DESIGN-3` |
| `FE-4` | `DESIGN-4` |
| `FE-5` | `DESIGN-5` |

</details>

<details>
<summary><strong>CI-N — CI/CD y automatización</strong></summary>

| Antiguo | Nuevo |
|---|---|
| `FIX-CI-1` | `CI-1` |
| `FIX-CI-2` | `CI-2` |
| `FIX-CI-3` | `CI-3` |
| `FIX-CI-4` | `CI-4` |

</details>

---

*Optimizaciones de este documento — **11 ago** (líneas exactas al
cierre de esta pasada, ver historial de git): la sección de apertura
("Hallazgos realmente abiertos", ~110 líneas) se condensó a un resumen
de ~20 líneas — casi todo su contenido (verificación de rate limiting,
la auditoría de estrés del 10 ago, el drift paralelo de 4 migraciones)
ya vivía palabra por palabra en § Historial cronológico y en las filas
`SEC-37`/`SEC-38`/`ARCH-42`/`ARCH-43`; nada se eliminó sin que ya
estuviera migrado a otra parte del documento, mismo criterio que las
pasadas anteriores. De paso, primera verificación en vivo del proyecto
real vía conector Supabase directo (antes de esta sesión el trabajo
era solo contra código/migraciones estáticas clonadas del repo):
`SEC-37` reconfirmado sin cambios de estado; nuevo hallazgo `SEC-39`
(3 items menores del advisor nativo de seguridad de Supabase, nunca
antes cruzado contra este índice). **16 jul** (601→~400 líneas): párrafos
de verificación de cada hallazgo ✅ cerrado condensados a causa raíz + fix
en una línea (migraciones/archivos/IDs intactos); historial narrativo
recortado a resumen por fecha. Incluyó una recuperación de contenido
(commit `44b25f6` con `SEC-25`/cierre de `SEC-23`, sobrescrito sin querer
por un push con la versión optimizada — reconstruido del diff real, sin
pérdida de información). **3 ago** (530→~450 líneas): mismo criterio
aplicado a las 4 secciones narrativas del "2 de agosto" que habían vuelto
a crecer arriba del archivo — replegadas a sus filas en las tablas por
prefijo (`ARCH-32`–`34`, `OFF-9`, `UX-25`–`28`) y a una línea en §
Historial; las 2 notas de recuperación de contenido perdido (commit
`4d9ca67`) se condensaron sin perder la referencia a los commits
involucrados. **6 ago** (532→~430 líneas): mismo criterio aplicado a la
sección de apertura (3 notas de proceso largas → bullets), la fila
`UX-30` (el bloque más grande del documento, 3 follow-ups completos →
causas raíz + cifras), la nota de reconciliación de `SEDE-N` y el
historial cronológico completo; las 2 notas HTML de recuperación de
contenido (`4d9ca67`/`fa450ea`, ya resueltas hace semanas) se retiraron.
A mitad de esta pasada, `git fetch` detectó 5 commits nuevos en paralelo
de LS (`UX-32` con código idéntico al ya aplicado acá, más `SEDE-15`
real — `PlanillaQR.jsx` sin filtro de sede) — la optimización se rehizo
desde cero sobre `origin/main` actualizado para no perder esa entrada.
Nada se eliminó sin migrarlo antes a su tabla permanente. Última
reorganización de fondo (normalización de IDs a 8 prefijos únicos): 14
de julio de 2026. Para el índice de migraciones SQL y el esquema de BD,
ver `ESQUEMA_Y_MIGRACIONES.md`.*
