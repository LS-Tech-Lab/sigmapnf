node_modules
dist
.DS_Store
.env
.env.local

# Playwright (Fix U-10) — artefactos de corrida, no las imágenes base
# (tests/visual/__screenshots__/ SÍ se commitea)
test-results/
playwright-report/
blob-report/
playwright/.cache/
